# POLISH_BACKLOG — ליטוש עיצוב לפני בטא

> עיצוב על מסכים ריקים זה ניחוש. בונים תשתית ומסכים מלאים קודם; אוספים כאן כל
> פריט עיצובי קטן שנתקלים בו תוך כדי, ועוברים עליו במרוכז על מסכים צפופים לפני בטא.
> **אין לתקן פריטים כאן תוך כדי בנייה — רק לרשום.**

---

## פתוח

### טיפוגרפיה
- [ ] **פונט המספרים לא "מכשיר מדידה" מספיק.** Roboto Mono עדיין לא נותן את
      תחושת המד המכויל. לבחון: משקל, `letter-spacing`, אולי גופן mono אחר
      (JetBrains Mono / IBM Plex Mono / Geist Mono). — *הבעלים, סבב ליטוש*
- [ ] **תווית "קריאה בלבד" בכרטיס הארכיון — גדולה מדי ולא במקום.** צריכה להיות
      תווית קטנה ומאופקת (chip/eyebrow), לא כותרת ענקית. — *הבעלים, סבב ליטוש*

### אווירה כללית
- [ ] **עדיין לא "2026" מלא.** האסתטיקה טובה אבל לא לגמרי עכשווית. לבחון בסוף
      על מסך צפוף אמיתי (הפקות/כספים), לא על מסך ריק. — *הבעלים, סבב ליטוש*

### נמצא תוך כדי בניית מסך הפקות
- [ ] **EntityDrawer עדיין באימוג'י וצבעי tailwind גולמיים.** `🎬💰⏳👤📄` +
      `text-red-400/emerald-400/amber-400` + `text-[var(--fg)]` (טוקן לא קיים).
      להחליף ל-`LineIcon` ולטוקנים של DESIGN. — `src/components/EntityDrawer.tsx`
- [ ] **הקפאה בתוך ה-drawer היא boolean פשוט** — לא לוכדת סיבה/מי/מתי כמו
      בכרטיס בלוח. לאחד לזרימת ההקפאה של הלוח. — `entities.ts` / `EntityDrawer`
- [ ] **קנבן חוסם תצוגה ל-60 כרטיסים לעמודה** (בלי virtualization). עמודת
      "עתיד להתחיל" ענקית (709). לשקול virtualization/pagination. — `ProductionsClient.tsx`
- [ ] **סטטוס ב-drawer הוא readonly** (משתנה רק בגרירה בלוח). לשקול בקרת סטטוס
      גם ב-drawer לנוחות. — `src/lib/entities.ts`
- [ ] **מסך הפקות חסר תצוגת יומן** (תצוגה ג׳ באפיון) — נדחתה בכוונה לשלב 6
      בסדר הבנייה. — *screens-spec §2, build order*

## פערי דאטה (לא עיצוב — להחלטת הבעלים)
- [ ] **🔵 "הופק ולא חויב" = 0 כי השלבים לא מולאו מההיסטוריה.** כל 4,254
      השלבים ב-`pending` (הזריעה אתחלה הכל), אז אף הפקה אינה "הופקה"
      בדאטה — ההתראה נכונה לוגית אבל חשוכה. כדי להאיר אותה צריך למלא את
      התקדמות השלבים מעמודת "סטטוס" ב-CSV המקורי (בעריכה/הופץ/וכו').
      **החלטת בעלים נדרשת** — לא לנחש 4,254 שורות. — *נמצא בבניית הרדאר*
- [ ] **שלב תקוע / מוקפא מעל 14 יום = 0** מאותה סיבה (אין in_progress, אין
      הקפאות ותיקות) — יאירו כשהצוות יעבוד עם הלוח. — *רדאר*

---

## נמצא בבניית מסך הייבוא
- [x] **107 שורות חשבונות היסטוריות לא במעקב — תוקן 2026-07-16.** archive.jobs
      אכן הכיל 106 מהן, רק בלי external_id (הועברו ב-step2 לפני 0014). מיגרציה
      0015 + backfill_archive_job_ids.py מילאו את כל 106. השורה ה-107 (C0050)
      הייתה חסרה לגמרי משני המקומות — נוצרה ידנית ב-archive.jobs (0016,
      insert_archive_job) כי paid='כן'+חשבונית מס מקיימים בדיוק את כלל
      הארכוב שכבר סיווג את 106 האחיות שלה. מאומת: archive.jobs=107, 51+106+1=157.
- [ ] **זיווג "תאומים" ב-backfill היה שרירותי.** ~12 שורות תאומות (אותה
      תוכנית+תאריך+אולפן, נבדלות רק במספר פרק) קיבלו external_id שרירותי,
      ולכן היבוא הראשון "התאם" אותן ל-CSV (מקור האמת). זה תקין אבל לא אלגנטי;
      אפשר לשפר את הזיווג ב-backfill (מיון לפי הערות/מספר פרק) למצב התחלתי נקי.
- [ ] **מסך ייבוא לא על ה-hub** — רק קישור בכותרת ל-can_import. לשקול כרטיס.
- [ ] **החלטות ארכיון (שחזר/עדכן) לא מחוברות** — הארכיון עכשיו מאוכלס (107),
      אבל ה-UI עדיין תומך רק ב"דלג" בפועל. לחבר restore/update. — `api/import/apply`
- [x] **יצירת שורה חדשה לא בדקה את כלל הארכוב — תוקן 2026-07-16.** `buildPlan`
      (merge.ts) מסמן `archiveDestined` על שורות `new` מסוג job כש-paid='כן'
      וחשבונית מס לא ריקה — **כבר בתצוגה המקדימה**, לא רק ב-apply. ב-apply,
      שורה כזו נוצרת ישירות ב-archive.jobs (RPC `insert_archive_job`, 0016)
      במקום ב-public.jobs. תוך כדי גם תוקן: שורה חדשה חיה (לא ארכיונית) לא
      ייבאה את "שולם" מה-CSV כלל (נפלה ל-'לא ידוע' תמיד) — עכשיו מיובא לשורה
      חדשה בלבד (לא לעדכון של שורה קיימת — שם עדיין מוגן, לא נדרס).
      מאומת: paid+חשבונית → ארכיון; paid='לא' → חי עם paid נכון.
- [ ] **התאמת שם לקוח לא עמידה בפני מיזוגי לקוחות.** `buildNameLookup` לחיוב
      חדש עושה התאמה מדויקת בלבד; שם לקוח היסטורי שמוזג (כמו "ידיעות אחרונות
      - אור נתן") לא נמצא יותר ונכשל בשקט (`applied.skipped`). לקוחות אין להם
      aliases כמו תוכניות — לשקול להוסיף. — *נמצא ביישום C0050*

## נמצא בבניית סנכרון היומן
- [x] **Cron ב-06:00 לא מדויק בחורף — נפתר.** `vercel.json` כבר מריץ שני
      crons (`0 3 * * *` + `0 4 * * *` UTC), וה-route בודק בפועל
      `israelHour !== 6` ומדלג על כל קריאה שלא נחתה על 06:00 ישראל — כך
      שהאוטומטי תמיד רץ ב-06:00 בדיוק, קיץ וחורף כאחד. אושר מול הבעלים
      2026-07-17 (מסך ההגדרות, אזהרת הפעלת הסנכרון): הטקסט אומר "06:00"
      כי זו השעה האמיתית שבה הסריקה רצה.
- [ ] **"יורש מחיר" חלקי.** תוכנית עם `default_rate` לא מעתיקה אותו לשום
      מקום על ה-production עצמו (אין עמודת מחיר על productions) — הירושה
      קיימת רק דרך `show_id`, וה-job שנוצר באישור לקוח עדיין לא ממולא
      אוטומטית מ-`default_rate`. לא תוקן; יורש client/kind/עורך/אולפן/צבע
      כן ממומשים במלואם בסנכרון. — *נמצא בבניית היומן*
- [ ] **ical.js הוחלף ל-node-ical** — node-ical (Temporal polyfill) שבר גם
      build-time analysis וגם runtime ("o.BigInt is not a function") בפייפליין
      הזה. ical.js עובד נקי, בלי תלויות Temporal. אם משדרגים חבילות בעתיד —
      לזכור שזו הסיבה לבחירה, לא רק "ical.js קליל יותר". — *נמצא בבניית היומן*

## איך למלא
כל פריט: מה נצפה · איפה (קובץ/מסך) · מי ביקש/מתי. בלי לתקן עכשיו.
