# Handoff — 17.9.2026

**להתחלת צ'אט חדש. יום השיוך והגארדים: שתי מיגרציות (0087, 0088), ארבעה שיוכים ידניים, ושלושה גארדים חדשים במסלול החיוב — `paidNoTax` ירדה מ-9 ל-0. המשימה הראשונה למחר עדיין כפיר ארביב: שירי טרם הוציאה את ה-320.**

---

## ⚠️ קודם כול — שלושה ריפואים ב-workspace

| ריפו | מה זה |
|---|---|
| **`/Users/admin/bizi-app`** | ← **כאן כל העבודה** |
| `/Users/admin/bn-invoice` | פרויקט אחר. **ה-cwd של הטרמינל** |
| `/Users/admin/attent-crm` | פרויקט אחר |

כל פרומפט נפתח ב-`cd /Users/admin/bizi-app && pwd && git rev-parse --abbrev-ref HEAD`. החזיק גם ב-16.9: אפס תקריות ריפו.

---

## איך אנחנו עובדים

**הבעלים אינו מתכנת.** בעלים → קלוד באפליקציה (מנסח פרומפט) → הבעלים מדביק לקלוד קוד → קלוד קוד מבצע.

- פרומפט אחד בכל פעם, ותחקיר לפני קוד בכל נתיב שנוגע בכסף. ב-16.9 זה עבד שלוש פעמים ברצף: התחקיר על חברת החשמל, על ליעד הרמן, ועל B8 — ובשלושתם **התחקיר הפריך את ההשערה שפתחה אותו**.
- **הבעלים מריץ את המיגרציות**, ואז שאילתת אימות נפרדת. `Success. No rows returned` אינו הוכחה — רק שורת הפנקס ב-`public.schema_ledger`.
- **הבעלים דוחף בעצמו.** `git push` נחסם ע"י מסווג ההרשאות של Claude Code — תקין (כלל 30), לא להוסיף הרשאה כללית. **נכון לסגירת 16.9: `origin/main` = `3b7b85c` = `HEAD`, הכול דחוף.**
- ניסוחים שמופיעים על המסך מאושרים לפני קוד.
- **הרצה מדומה לפני כל מיגרציה:** בלוק `DO` שמסתיים ב-`raise exception` (הגלגול מובטח ע"י PostgreSQL, לא ע"י ה-API). Management API **אינו מחזיר NOTICE** — הערכים מחושבים מחדש בתוך הטרנזקציה.
- **כשלבעלים יש מסגרת עסקית, היא קודמת לניתוח.** ב-16.9 זה הכריע שלוש פעמים: 50041/70006 אצל חברת החשמל, מחיר החצי אצל ליעד, והפרו-בונו אצל חתונמיות.
- **כלל 50 🆕 — שאילתות אימות של מיגרציות נשמרות ב-`supabase/verify/`, לעולם לא ב-`supabase/migrations/`.** בתיקיית המיגרציות רק קבצים שמוחלים על המסד.

---

## 🎯 שבע המשימות למחר

### 1. כפיר ארביב — עדיין פתוח, שירי טרם הוציאה
**נבדק 16.9 בערב: 40283 ו-40289 עדיין `type=300, status=0`.** שירי מוציאה **חשבונית מס קבלה (320), ₪590** על כל אחת. **לא 305** — 305 סוגרת את חשבון העסקה אבל פותחת חוב חדש ואינה מסמנת שולם.

אחרי ההנפקה — **שתי השאילתות מ-[`HANDOFF-2026-09-16.md`](HANDOFF-2026-09-16.md), משימה 1**, ללא שינוי. צפוי: שלוש שורות `paid = כן`, הפקות 1 · 2 · 2; ואחרי משיכת 05:00 — `status = 1`, `ref = []`.

אם `paid` נשאר "לא" — לבדוק `auto_tax_receipt_paid_not_flipped` ב-`events`. **עדיין ההרצה החיה הראשונה של האירוע.**

### 2. בר ביצוע — הקבלה על 50070 (רק כשהכסף הגיע)
`50070` נבדק 16.9: `type=305, status=0, ₪49,560`, job `ff089529`. **לא ללחוץ לפני שהתשלום התקבל — בלתי הפיך.** ההנפקה הראשונה של קבלה על אבן דרך.

### 3. 🆕 חברת החשמל — 50066 מחכה לתשלום
`50066` — **305, ₪1,534, `status=0` (פתוחה) מ-12.7, `job_id` ריק.** היא שייכת ל-job `8a1bcf1c` (₪1,300 · 13.8 · `paid='לא'` · `invoice_biz=40287`).

**כשהלקוח ישלם ושירי תוציא קבלה — לשייך גם את 50066 וגם את הקבלה ל-job.** עד אז ה-job נשאר `paid='לא'`, **וזה נכון** — הכסף לא נכנס.

⚠️ **את 40287 כבר שייכנו ידנית ב-16.9 20:25.** זה מה שהעיף את ה-job ממצב סגול לכחול, וזה גם מה שהופך את החסימה שלו היום לכלל a ולא לכלל c2.

### 4. 🆕 ליעד הרמן 28.8 — 40311 פתוח
`40311` — **300, ₪354 ברוטו (₪300 נטו אחרי הנחת התקלה), `status=0`**, job `df5758d7`. **חשבונית מס רק כשישולם.** שלוש השאלות הפתוחות (מי ההפקה השורדת · האם ₪300 סופי · חשבונית המס) נשארות ב-**P7** — 0088 החזירה את המיזוג ו**אינה** מכריעה באף אחת מהן.

### 5. 🆕 שתי שורות בתור `/documents`
נבדק 16.9 בערב, בדיוק שתיים:

| שורה | מצב | מה לעשות |
|---|---|---|
| **ידידיה ויטל, הזמנת עבודה ₪1,100** | `pending`, מ-06:00 | נושאת אזהרת **"אורח חסר בפירוט"** — לערוך לפני אישור |
| **גל אורן לרנר, 320 ₪1,500** | `failed ×1` | השגיאה המדויקת: **`נא למלא את פרטי הצ׳ק בשורת התקבול`**. **למלא לפני "נסה שוב"** — אחרת ייכשל שוב |

### 6. סבב רישום — חשבונות עסקה פתוחים בלי עבודה
משימה 3 מ-[`HANDOFF-2026-09-16.md`](HANDOFF-2026-09-16.md). **למדוד מחדש לפני שמתחילים** — 16.9 הזיז הרבה שורות.

### 7. הבא בתור בעבודת קוד: **BL-2 — `dueDate`**
ההכרעות נעולות; נשאר סקריפט אימות TS מול `compute_due_date`. הפירוט ב-[`BACKLOG-V1b.md`](BACKLOG-V1b.md), פריט 2.

---

## מה נסגר 16.9

### מיגרציות — **0088 היא האחרונה**

| # | מה | הוחלה (זמן ישראל) |
|---|---|---|
| **0087** | סגירת שלושת הבנדלים שנמשכו — 60171 (חתונמיות, ₪4,200 נטו) · 60178 (ליעד, ₪1,500) · 60183 (ברק, ₪1,800). 3 jobs חדשים, נטע צמח ל-`'ללא חיוב'`, כפילות ליעד 1.7 (`2e651251`) הוסתרה. **`paidNoTax` 9 → 0.** | 16.9 **19:55:22** |
| **0088** | החזרת המיזוג של 0065 — `94d4a7ea` → `6bd7289f` (ליעד, 28.8), שטכנאי ביטל ב-8.9. **עמודה אחת בשורה אחת.** | 16.9 **21:43:06** |

**אימות 0088 — כל עשר הבדיקות `true`:** `dup_remerged` · `one_live_production` · `survivor_untouched` · `dup_job_still_dismissed` · `keep_job_untouched` · `dup_wo_still_rejected` · `no_new_queue_rows` · `docs_intact` · `ledger_row` · `audit_event`.

⚠️ **פער החוב של 0087 — `+1,100` — אינו 0087.** הוא job `f78be4bb` (ידידיה ויטל · "המניפה" · ₪1,100 · `paid='לא'`) שנולד **19:41:45** ממעבר הפקה ל"הוקלט", **שתי דקות לפני** ש-0087 רצה.

### קוד — כל הקומיטים דחופים ל-`main`

| SHA | שעה | מה |
|---|---|---|
| `c70dfc4` | 01:28 | סנכרון `TICKETS.md` ל-16.9 |
| `dce0681` | 01:46 | סידור התור ב-`BACKLOG-V1b.md` |
| `c2cafe7` | 01:41 | לוגו לבן ב-`public/brand/` |
| `a1cbf32` · `c099d22` · `c6e966c` | 14:16–14:50 | **B8** — פרדיקט `paidNoTax` אחד, יישור כרטיס הבית, `?filter=paid_no_tax` ו-`amount_missing` (`FINANCE_FILTERS`) |
| `94ae0b0` · `ec9f493` | 15:21 · 15:48 | **B10** — איפוס דגלי אישור בגרירה אחורה בקנבן. השני הוא התיקון לאישור חלקי |
| `c8c15cd` | 20:30 | **B12** — כלל **c2** + תג "נדחה בעבר" על כרטיס בתור |
| `c5bff9e` | 21:00 | קובץ **0088** |
| `45b5c2a` | 21:02 | ביטול מיזוג — דרג כספים מותנה |
| `4650564` | 21:05 | **B13** — כלל **d** |
| `ca46610` | 21:49 | **ביטול מיזוג = כספים תמיד** (הכרעת בעלים) |

**B8 — ⚠️ ההשערה המקורית לא שוחזרה.** שלושת ה-jobs של דינמיקס איזון **נושאים** `invoice_tax='60192'`, ונתיב הבנדל האפליקטיבי תקין. הסיבה האמיתית להתראות השווא — מסמכי מס שנמשכו ואינם מקושרים — נפתחה כ-**F9**.

**B10 — ⚠️ אחרי גרירה אחורה יש לשלוח לינק חדש.** הלינק הישן נשאר נעול (`responded_at`), **בכוונה**: לינק נענה פעם אחת.

**B12/c2 — B10 חשף את הבאג, לא יצר אותו.** הגרירה אינה נוגעת ב-`jobs`/`documents`/`pending_documents`; היא החזירה את ההפקה למצב שבו אישור לקוח יכול לירות שוב, וזה תפקידה.

### נתונים שנרשמו ידנית דרך הרג'יסטרי

| מסמך | שויך ל | הערה |
|---|---|---|
| **60143** (320, ₪3,540) | `7fad9fff` — **דה פקטו** | שחף סגינר הוא **איש קשר**, לא לקוח נפרד |
| **60186** (320, ₪1,416) | `591b363e` — **סבטלנה ניקסון** | הסכום תוקן **2,400 → 1,200** |
| — | `22618f53` — **ברק הרשקוביץ** | הסכום תוקן ל-**₪600** |
| **40287** (300, ₪1,534) | `8a1bcf1c` — **חברת החשמל** | 16.9 20:25 |

### החלטות שנסגרו מחוץ לקוד

**חברת החשמל — חשבונית המס הכפולה הישנה סגורה.** `50041` (305, ₪708) **בוטלה** (`status=4`) בחשבונית זיכוי `70006` ב-6.1.2026. אומת בנתונים. **סגור, לא לפתוח מחדש.**

---

## הכרעות שנקבעו — לא לפתוח מחדש

כל תשע ההכרעות מ-[`HANDOFF-2026-09-16.md`](HANDOFF-2026-09-16.md) (א–ט) **עומדות ללא שינוי**. נוספו:

**י. 🆕 ביטול מיזוג הוא פעולת כספים, תמיד.** טכנאי שמיזג בטעות פונה לבעלים. הדרג **המותנה** של `45b5c2a` (job מוסתר / מסמך נדחה) הוחלף ב-`ca46610` בדרישה גורפת — הבדיקה רצה **ראשונה**, לפני קריאת השורה. **המיזוג עצמו (POST) נשאר בדרג השלבים.** הנימוק: הגרסה המותנית הגנה על הכסף שהמיזוג כבר הזיז, ולא היה לה מה לומר על הכסף שהביטול **עומד לשחרר**.

**יא. 🆕 גארד חוסם, לעולם אינו מקשר.** c2 ו-d שניהם מחזירים `{status:"exists"}` ואינם כותבים `job_id`. קישור הוא הכרעה אנושית ב-`/documents/gaps` — אותו קו ש-0087 מתחה.

**יב. 🆕 "ודאית" יש לה הגדרה אחת בקוד.** c2 מסנן את הקשתות ש-`buildEdges` כבר בונה ומוסיף רק ייחודיות 1:1 וחלון ±45. כלל שני משלנו היה אמת שנייה על אותה מילה.

**יג. 🆕 0088 אינה תקדים לביטול-של-ביטול.** מה שהכשיר את הכתיבה הוא ששלושת החלקים האחרים של אותה החלטה עצמה עדיין עמדו על המסד — הנתון סתר את עצמו ויישרנו אותו לרוב. **לא** שדעתנו טובה משל מי שלחץ.

---

## מלכודות

כל המלכודות מ-[`HANDOFF-2026-09-16.md`](HANDOFF-2026-09-16.md) **עומדות** — במיוחד `MORNING_DRY_RUN=false`, 305 מול 320, `paid='לא ידוע'` ≠ `'לא'`, `documents.ref` הוא `raw->'ref'`, המשיכה ב-05:00, וכלל 41 (עצירת שרת דב לפני `build`). חדשות מ-16.9:

**🆕 🔴 הסתרה אינה שיוך.** עבודה מוסתרת נעלמת מההתראות — **הבעיה נשארת**. זה היה המוצא היחיד שנותר לבוקקיפרית, וזה **Q9**: הכלי החסר הוא הבעיה, לא מי שהשתמש במוצא.

**🆕 🔴 אל תלחץ "הנפק חשבונית מס" במסך הכספים על שורה שיש לה כבר חשבונית במורנינג.** **לשייך קודם.**

**🆕 ⚠️ שיוך מסמך למסמך אינו שיוך לעבודה.** `parent_doc_numbers` מקשר 305→300→100 ואינו נוגע ב-`documents.job_id`. **305 מוצעת רק לעבודה אדומה** (ששולמה ואין לה חשבונית מס) — לעבודה סגולה היא לא תוצע לעולם.

**🆕 ⚠️ לקוח ממוזג יכול להשאיר מזהה מורנינג יתום.** `fcf1e261` — **4 מסמכים** נושאים אותו, המוקדם 13.5. כל משיכה עתידית על שם שחף סגינר תגיע **לא משויכת**. זה **F10**.

**🆕 ⚠️ חלון השיוך במסך הכספים ריק כשאין מפתח לקוח משותף** — והרג'יסטרי **כן** מציע לפי סכום. אותה שאלה, שתי תשובות. זה **F9**, והוא הצעד הזול הראשון שם.

**🆕 ⚠️ "באיחור N יום" מוצג גם על עבודה ששולמה** — `finance/page.tsx:76` מחשב `due_date − now` בלי לבדוק `paid`. גם בייצוא ה-CSV. זה **B11**.

**🆕 ⚠️ ביטול מיזוג מנקה `merged_into` בלבד.** job מוסתר נשאר מוסתר, מסמך נדחה נשאר נדחה. מעכשיו הפעולה דורשת `can_edit_money` **תמיד**, והאירוע `production_merge_undone` נושא `not_restored`.

**🆕 ⚠️ תאריכים.** ב-16.9 נכתב בטעות "17.9" בכמה מקומות ותוקן (`ef466c7`). **לוודא את התאריך מול המערכת לפני כתיבת מסמך.**

---

## בקלוג, לפי דחיפות

הבקלוג מ-[`HANDOFF-2026-09-16.md`](HANDOFF-2026-09-16.md) עומד. **נסגרו 16.9:** פריט 4 (`paidNoTax` — B8, אם כי לא מהסיבה שנכתבה) · פריט 12 (חברת החשמל 50066 — עלה למשימה 3 למחר).

**🆕 נפתחו 16.9:**

1. **F9 🔴 — קישור מסמכי מס שנמשכו ל-job דרך `parent_doc_numbers`**, ותיקון הסימטריה `job→doc` מול `doc→job`. 7 מסמכי מס חיים במצב הזה. **לא לבנות עד שהמקרים הקיימים נבדקו ידנית** — 7 מסמכים הם חצי שעה של אדם; מנגנון שיטעה באחד מהם כותב מספר חשבונית שגוי על עבודה.
2. **Q9 🔴 — כלי לשיוך בנדל שנמשך.** אין מסך שמשייך מסמך שנמשך לכמה עבודות; "שייך מסמך קיים" עובד עבודה-אחת-למסמך-אחד. זה מה שאילץ את 0087 להיות מיגרציה.
3. **T6 — מיגרציה ממוקדת ל-`documents.job_id`** (40261 / 60181).
4. **F10 — מזהי מורנינג כפולים ללקוח** (`fcf1e261`, 4 מסמכים).
5. **B11 — "פירעון" מציג איחור על עבודה ששולמה.** תיקון תצוגה, לא נוגע בכסף.
6. **P7 — שאלות פתוחות על ליעד 28.8.** מי ההפקה השורדת · האם ₪300 סופי · חשבונית המס.
7. **`deal_invoice_skipped_already_billed` עם `needs_link: true` — אין לו שום קורא.** לא `/documents/gaps` ולא הרדאר. **חסימה אינה סגירה**: c2 מונע כפילות ומשאיר את הפער פתוח, והאירוע נכתב כדי שיהיה ניתן להצגה ביום שמסך יבקש אותו.

---

## מפתחות ומספרים

- **DB:** `teobjwdszasavvmvukfb`, פרנקפורט. פנקס: `public.schema_ledger` (לא `supabase_migrations`).
- **המיגרציה האחרונה: `0088`.** הבאה היא `0089`.
- **`ff089529`** — בר ביצוע · **`e3ea418d`** — הלקוח · **`50070`** הקבלה הראשונה על אבן דרך.
- **`ac4a4520`** — כפיר ארביב · **`4388cb08`** ו-**`f3d44265`** — ה-jobs של 40283 ו-40289.
- 🆕 **`8a1bcf1c`** — חברת החשמל, 13.8, ₪1,300, `invoice_biz=40287`, `paid='לא'`. **`50066`** מחכה לתשלום.
- 🆕 **`6bd7289f`** — ההפקה השורדת של ליעד 28.8 (של היומן, UID `4705C10E`) · **`94d4a7ea`** — הכפילות, ממוזגת שוב · **`df5758d7`** ה-job (₪300, `40311`) · **`8ce89e57`** ה-job המוסתר (₪600).
- 🆕 **`f78be4bb`** — ידידיה ויטל, ₪1,100, נולד 16.9 19:41:45. **הוא כל פער החוב מול 0087.**
- 🆕 **`fcf1e261`** — מזהה המורנינג היתום של שחף סגינר.
- **חוב לגבייה — למדוד מחדש, לא לצטט.** 16.9 הזיז הרבה שורות.
- 🆕 **משתמשים שיכולים לבטל מיזוג:** בניהו · שירי · אלי (שלושתם `owner` + `can_edit_money`). **שחר ו-gilhasid43 — לא.**

---

## הלקחים של היום

**1. התראה שמושתקת בהסתרה מסתירה חשיפה אמיתית.** שבע שורות `paidNoTax` נעלמו כי הבוקקיפרית הסתירה אותן — וזה היה המוצא **היחיד** שנותר לה, כי אין מסך שמשייך מסמך מאוגד שנמשך. **הכלי החסר הוא הבעיה, לא מי שהשתמש במה שהיה.** כשרואים דפוס של עקיפה ידנית, לחפש את הכלי שחסר לפני שמחפשים את הטעות.

**2. תיקון שפותח מחדש זרימה חושף כל גארד שנשען על מצב שהזרימה הניחה.** B10 החזיר לאישור לקוח את היכולת לירות שוב, ומיד נפלו ארבעה כללים ששתקו — לא כי B10 שבר אותם, אלא כי עד אז שום דבר לא ביקש מהם לענות פעמיים. **אחרי שפותחים מסלול, לבדוק מה לאורכו הניח שהוא נסגר.**

**3. ביטול צריך לדרוש לפחות את מה שהפעולה המקורית דרשה.** המיזוג של ליעד לא היה נגיש בממשק כלל; הביטול שלו היה קליק אחד לטכנאי. **אי-סימטריה בין פעולה לביטולה היא באג הרשאות גם כשכל צד בנפרד נראה סביר.**

**4. הזיכרון של הבעלים פתר את מה שהנתונים לא יכלו.** 50041/70006 (חשבונית שבוטלה בזיכוי לפני תשעה חודשים), מחיר החצי אצל ליעד (הנחה על תקלה טכנית), והפרו-בונו אצל חתונמיות — **שלושתם לא היו נגזרים משום שאילתה.** כשהנתונים מראים חור, לשאול לפני שמסיקים.

**5. תחקיר שמפריך את ההשערה שפתחה אותו הוא תחקיר שהצליח.** שלוש פעמים היום: B8 (לא בנדלים), חברת החשמל (לא הקנבן — הלקוח), ליעד (לא הסנכרון — ביטול ידני). **בשלושתם, אילו היינו כותבים קוד לפי הכותרת, היינו מתקנים את הדבר הלא נכון.**
