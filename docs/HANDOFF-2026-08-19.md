# Handoff — בוקר 19.8.2026
**להתחלת צ'אט חדש. סיכום שלושת ימי העבודה 16–18.8, ומאיפה ממשיכים.**

---

## הכי חשוב — מאיפה מתחילים מחר

הבחירה בין שניים, שניהם דורשים ראש צלול (נוגעים במורנינג/מסד/כסף):

1. **D3 — אפיק התקרה.** הדבר הגדול, יש לו שעון: ~27 יום ל-40258 (ידיעות, "ביקור ב'", 250K נטו, חשבונית 320 ב-15.9). דורש אפיון לפני קוד.
2. **V1b — שם אורח למורנינג.** משלים את שרשרת האורח שהתחלנו (V1a כבר בוצע). קטן יותר מ-D3 אבל נוגע ב-enqueue ובטריגר DB 0033. אם רוצים לסגור את V1 לפני שעוברים לאפיק גדול.

**המלצה:** אם הראש רענן — D3 (השעון). אם רוצים ניצחון מהיר ואז אפיק — V1b קודם.

**תזכורת שלא לשכוח:** **D2 — השהיית סינגפור — חמישי 21.8.** השהיה, לא מחיקה. `.env.local.singapore` נשאר.

---

## מה נסגר בשלושת הימים (הכל ב-git, נדחף)

**באגים ותיקונים:**
- B1 — לינק צפייה שמת: supersession גלובלי + `review_reels_required` גלובלי. תוקן scope-aware.
- B2 — כפתור מאוחד (פרק+רילז) תמיד גלוי, תוויות משקפות scope.
- B3 — איפוס הערת תיקונים בסיבוב חוזר (נבלע ב-1ב, `last_note` פר-פריט).
- Q1 — הוסר מחיר הפרק מהדף הציבורי (מחירי תוספות נשארו — הלקוח מאשר אותם).
- Q7 — מלכודת כפתור השליחה: קופסת לינק חי + "העתק קיים" + אזהרת דריסה פר-scope.
- B7 — באג URL/SSO: לינקי צפייה והזמנות נבנו מ-request.url → דומיין preview מוגן ב-Vercel SSO. תוקן ב-`getAppBaseUrl()` + `NEXT_PUBLIC_APP_URL`. **וגם:** Supabase Site URL היה `localhost:3000` (!) ו-Redirect URLs ריק — תוקנו.
- באג Data Cache → **כלל 39**: כל קליינט supabase בצד שרת עובר דרך `noStoreFetch`.
- באג הטריגר → **מיגרציה 0058**: `trg_handle_new_user` אבד בהגירה סינגפור→פרנקפורט. הפונקציה שרדה, ה-binding לא.
- באג ה-LOCATION: כתובת רחוב דרסה את `default_studio` (20% מההפקות). תוקן ב-V1a.

**פיצ'רים:**
- **E2 (מסך אישור לקוח מבוסס פריטים):** שלב 0 (נגן דרייב מוטמע) ✅, 1א (מודל `client_review_items`, מיגרציה 0057, תיבה+נגן פר-ריל) ✅, 1ב (אישור והערות פר-פריט, respond ב-item-mode, מאומת e2e) ✅.
- **מיגרציה 0059** — שלב הקלטה להפקות רילז-בלבד (`create_default_stages` + `derive_production_status`).
- **V1a** — קונבנציית כותרת יומן: `אורח:`/`אורחת:` → guest (עד פסיק), זיהוי אוטומטי של אולפן (גבעון/גבעון גדול/חשמונאים + וריאציות) → studio. `bba6103`.
- **מיתוג** — Bizi Podclub בכל המערכת.

---

## מה פתוח (הכל ב-docs/TICKETS.md)

**שרשרת האורח (המשך V1a):**
- **V1b** — שם אורח למורנינג: `guest` חסר ב-`ProductionForBilling` (enqueue.ts:30-43), + טריגר DB 0033 (`campaign=podcast_name`, מאבד אורח ברמת מסד). קוד + מיגרציה. **הבקשה: שם האורח בהערות של הזמנת העבודה.**
- **V1c** — סנכרון היומן לא מרענן `guest`/`studio` אחרי יצירה (route.ts, ענף העדכון). שינוי בכותרת לא מגיע להפקה.
- **P1-זנב** — פרסינג מספר מצלמות/מיקרופונים (`camera_count`). נפרד מ-V1a, לא דחוף.
- **מיגרציית ניקוי** — 5 שורות עם כתובת רחוב בשדה studio (מזיהום ה-LOCATION). קדימה כבר נקי; רטרואקטיבי אם תרצה.

**גדולים:**
- **D3** — אפיק התקרה (ראה למעלה). ~27 יום.
- **E2 שלב 3** — Google Picker, מסך-מלא לנגן, סימניות/טיימקוד.
- **E1 (רדיו), E3 (אינבוקס), E4 (סוכן AI)** — אפיקים גדולים, אפיון לפני קוד.

**רשימת ניקיון (קטן, לא דחוף):**
- `welcome/page.tsx:56` — `window.location.origin` בקישור התאוששות (תיקון שורה אחת, `NEXT_PUBLIC_APP_URL` זמין בדפדפן).
- Q6 — טוקני לינקים מאלפבית חד-משמעי (בלי I/l/O/0).
- היתום `ztestcancel_…` ב-profiles.
- אייקוני PWA / מיילי auth בדשבורד — אם נושאים מיתוג ישן.
- T16 — לבדוק אם אבדו עוד bindings לסכימת auth בהגירה (מקושר ל-0058).
- חוב בדיקות (T15) — test_client_review_link + test_upsells מצפים למחיר שהוסר (Q1) ולשלבים ישנים; לא מנקים client_review_items לפני מחיקת הפקה.

---

## כללי עבודה קריטיים (מ-CLAUDE / דוחות)

- **פרומפט אחד בכל פעם, דיווח לפני קוד** בנתיבים רגישים.
- **מיגרציות: SQL Editor בלבד** (אין `supabase db push`), `DO $$` עם גארד פנקס, `git add -f` (יש `*.sql` ב-gitignore), `schema_ledger` אחרי כל מיגרציה, רענון `database.types.ts`.
- **`npm run build` לפני כל push** (לא רק tsc — Vercel מריץ ESLint).
- **`git status -sb`** לאימות ענף. קלוד קוד לא תמיד מדייק על ה-ahead.
- **כלל 35:** הקרון הכפול של calendar/sync הוא כוונה (UTC+שעון קיץ) — לא למחוק.
- **כלל 38:** אחרי רישום אישור לקוח — שום דבר לא מפיל את הדף הציבורי (נרשם כאירוע, לא זורק).
- **כלל 39:** כל קליינט supabase בצד שרת → `noStoreFetch`.
- **חשבונאות:** הטריגר לחשבון עסקה חי על המעבר ל**אושר_ע"י_לקוח**. דילוג-סמן מעליו לא מצית (F7 — נבדק, אין backlog).

## מפתחות ומספרים
- **40258** — ידיעות, "ביקור ב'", 250K נטו, 320 ב-15.9 (מחכה ל-D3).
- **אסתטיטוקס** (`740d206e`) — הפקת הבדיקה המרכזית ל-E2 (פרק + 2 רילז).
- **הפקות רילז-בלבד:** מישל רילז, icr spotlight (הבדיקה ל-0059).
- **DB:** פרויקט פרנקפורט `teobjwdszasavvmvukfb`. Supabase Pro + Vercel Pro.
- **דומיין נוכחי:** `bizi-tech-and-invoice.vercel.app` (סאב-דומיין עתידי לפודקלאב מתוכנן, לא דחוף).

---

## דפוס שחוזר על עצמו (שווה לזכור)
כל שלושת הימים: **בדיקה ידנית אחרי כל טיקט חשפה באג פרודקשן אמיתי שלא היה בלוח** — הטריגר (0058), שרשרת ה-URL/SSO, באג ה-LOCATION, הכפילות ב-Q7. אל תדלג על בדיקות ידניות בפרודקשן, גם כשה-e2e ירוק.
