# Backlog — V1b: שם אורח + תאריך בשורת הפירוט

**עודכן לאחרונה:** 30.8.2026
**סטטוס נוכחי:** V1b (A+B) ✅. D3 ✅ — ממתין לתשלום ידיעות. פאנל החוזים ✅. אוטומציית 320/400→paid ✅. תיקון רדאר ✅. תיקון מנגנון יציאה בבדיקות ✅. תיקון בליעת כשל מורנינג בעדכון לקוח ✅. **ארכיטקטורת job (0060–0064) ✅** — ה-job נולד בהקלטה, לא באישור; ביטול פרק שהוקלט = אדמין; מסלול ההנחה סגור. **דגל "אורח חסר בפירוט" ✅** (`2b139ad`) על שני המסכים. **ניקוי זיהום V1a ✅** — 0 מתוך 325 אורחים הם שם אולפן. **מסך `/projects` ✅** (27.8). **סנכרון היומן מפספס אירועים חוזרים (RRULE)** — אובחן 26.8, הוכרע **לא לתקן**; התראה נבנתה. **🎯 30.8 — הפדיון הראשון בחיי המערכת יצא** (ברק, הזמנה מאוגדת 10317, 5 פרקים), ואיתו חמישה תיקונים שהוא עצמו חשף: מסלול `consolidated`, שם האורח בטופס, "צור חשבון עסקה" במרשם, וגארד הכפילות בשני הקצוות (0065/0066 + `60cbd2b` + `67aaa1c`). **הבא:** להמיר את 10317 לחשבון עסקה, בליעה ב-review/route.ts (כסף ישיר), ואז 3 רגרסיות בדיקה קלות.

---

## 📋 מיפוי מול לוח הטיקטים המקורי (30.8.2026)

**הסייג שקודם לטבלה.** הרשימה הגולמית של **25 הטיקטים של הבעלים אינה בריפו** — נסרקו כל קבצי ה-md, כולל מחוקים בהיסטוריית git. הקרוב ביותר הוא [`docs/TICKETS.md`](TICKETS.md) (נוצר 16.8, עודכן לאחרונה 19.8 ב-`4760d35`), ששורתו השנייה מצהירה: *"מקורות: דוח מצב ט"ז (15.8) + **רשימת 25 הטיקטים של בניה** + בקלוג קיים"*. הוא **מיזג שלושה מקורות בלי לסמן מוצא**, ולכן אי אפשר לומר אילו מהמזהים שבו הגיעו דווקא מ-25 של הבעלים.

> **✅ מה שכן נסגר (30.8): אין טיקט חסר.** הביקורת המקורית סימנה את *"מס' 7 מרשימת בניה — שמור, טרם מולא"* כראיה לכך שטיקט מהרשימה אבד. **הבעלים אישר שזו הייתה קריאה שגויה: הטיקט נמחק מהרשימה והמספור לא עודכן.** הפער במספור הוא שריד עריכה, לא חוסר. השורה הוסרה מ-`TICKETS.md` עם הערה במקומה, כדי שביקורת עתידית לא תחפש אותו שוב. **הסייג על המוצא נשאר בתוקף** — הוא על מיזוג המקורות, לא על הטיקט הזה.

**⚠️ = פיצ'ר שלא נגענו בו** (להבדיל מבאג). אלה מה שנופל בין הכיסאות במרתון תיקונים.

| ID | טיקט | מצב | ראיה / מה חסר |
|---|---|---|---|
| B1 | לינק צפייה נשרף | ✅ | 16.8, supersession מודע-scope |
| B2 | אופציית רילז+פרק נעלמה | ✅ | 16.8 |
| B3 | הערת תיקונים לא מתאפסת | ✅ | `f138f87` |
| B4 | חיפוש גלובלי נבלע (z-index) | ⬜ | לא נפתח |
| B5 | גלאי כפילויות ברדאר = אפס | ⬜ | אומת 30.8: [`alerts.ts:280`](../src/modules/radar/alerts.ts#L280) עדיין מקבץ לפי מפתח מדויק. שער ה-fuzzy לא נבנה |
| B6 | משתמש בלי `profiles` | ✅ | `e58e6d7` + 0058 |
| B7 | לינקים מדומיין preview | ✅ | `a6ba364`. **שארית:** [`welcome/page.tsx:56`](../src/app/welcome/page.tsx#L56) עדיין `window.location.origin` — אומת 30.8 |
| D2 | השהיית סינגפור | ✅ התייתר | השהיה חסומה ב-Pro, $2-6/חודש. הוכרע להשאיר |
| D3 | אפיק התקרה 320/305 | ✅ | `1c9c821`. ההוצאה ממתינה לתשלום 40258 |
| Q1 | הסרת סכום ממסך הצפייה | ✅ | 16.8 |
| Q2 | סינון רג'יסטרי לפי סגור | ⬜ | תלוי F3 |
| Q3 | סדר עמודות ברג'יסטרי | ⚠️ ⬜ | |
| Q4 | רדאר "חוב לגבייה" לחיץ | ⚠️ ⬜ | תלוי F3 |
| Q5 | MICRO → SMALL | ⬜ | אפס קוד, ~$10/חודש |
| Q6 | טוקנים מאלפבית חד-משמעי | ⬜ | אומת 30.8: [`links.ts:13`](../src/lib/review/links.ts#L13) עדיין `base64url` — I/l/O/0 בפנים |
| Q7 | מלכודת כפתור השליחה | ✅ | `a1c7385` + `1ba18b8` |
| F1 | ניכוי במקור — שתי שורות | ⚠️ ⬜ | 31 שורות בספרים. השער עוצר נכון, לא יטעה בשקט |
| F2 | סגירת חשבון עסקה ידנית + סנכרון | ⚠️ ⬜ | 30.8 סגר את ה**יצירה** (`210dbc3`), לא את הסגירה. נתיב כתיבה למורנינג פתוח |
| F3 | סטטוס פתוח/סגור + רטרואקטיבית | 🟡 | `readOpenness` קיים וחוסם בבנאי (3 עותקים), המרשם מציג `parentOpennessUnknown`. **חסר:** סטטוס נשמר + מילוי אחורה — ולכן Q2/Q4 חסומים |
| F4 | חשבונית ביודעין בלי job | ⚠️ ⬜ | חופף ל-T6 |
| F5 | "שולם" בלי אישור לקוח | 🟡 | `a6ca635` הפך 320/400→paid **אוטומטית**. המסלול הידני + הסימון בלשונית + העדיפות — לא |
| F6 | מחיר שעתי בתוכניות | ⚠️ ⬜ | דוידסון 250 ₪/שעה. פיצ'ר עצמאי, לא התחיל |
| F7 | מנע דילוג מעל אושר_ע"י_לקוח | 🟡 | שאילתת העקבות רצה 17.8 (אפס חוב כספי). ה**מנע** עצמו לא נבנה |
| P1 | מיקום מהיומן דורס קבוע | 🟡 | `bba6103` (V1a) מחלץ אולפן לכותרת→`productions.studio` + [`studios.ts`](../src/lib/calendar/studios.ts) עם האליאסים. **חסר:** מספר מצלמות/מיקרופונים, וטבלת אליאסים (זו רשימה בקוד) |
| P2 | מונה פרקים בלוח התוכניות | ⚠️ ⬜ | `/projects` (27.8) נותן טבלה כרונולוגית — סמוך, לא זה |
| P3 | הפקות פנימיות — זרימה נפרדת | ⚠️ ⏸️ | אפיון לפני קוד |
| P4 | שינוי מקום אחסון באמצע | ⚠️ ⬜ | |
| P5 | גוגל דרייב — העלאת קבצים | ⚠️ ⬜ | OAuth. "לא להתחיל בלי חלון זמן" |
| P6 | שלב הקלטה לרילז-בלבד | ✅ | `88d90da` + 0059 |
| V1a | אולפן/אורח מהכותרת | ✅ | `bba6103` |
| V1b | שם אורח למורנינג | ✅ | `873eb05` + `381601f` + `2b139ad` + `70488a5` |
| V1c | רענון guest/studio בסנכרון | ⬜ | שינוי כותרת ביומן לא מגיע להפקה |
| E1 | רדיו | ⚠️ ⏸️ | ממתין לפגישה עם אלי |
| E2 | אישור לקוח מבוסס-פריטים | 🟡 | שלבים 0/1א/1ב ✅. **חסר שלב 3:** Google Picker, מסך-מלא, סימניות/טיימקוד |
| E3 | אינבוקס | ⚠️ ⏸️ | Realtime + Web Push |
| E4 | סוכן AI | ⚠️ ⏸️ | "אחרון בכוונה". *(סיווג מסמכים ב-AI כן זז — בריפו `bn-invoice`, מערכת אחרת)* |
| T1 | dev→prod בלי רשת ביטחון | ⬜ | |
| T2 | Preview בלי מסד | ⬜ | |
| T3 | `MORNING_DRY_RUN` לא קריא | ✅ התייתר | `/api/morning/status` מחזיר `{dryRun}` מאז `8f543ee` (**20.7**) — הטיקט נכתב אחרי שכבר נפתר |
| T4 | service-role key מסומן Sensitive | ⬜ | |
| T5 | סכימת `jobs` — טיקט-על | 🟡 | 0060–0064 + `99d02e8` פתרו מחזור חיים ופתרון job דרך ההפקה. **חסר:** FK מול `documents`, 1:1 מול 1:N, שתי טבלאות `jobs` — ונוסף `archive.jobs` שלא קיימת |
| T6 | 34 מסמכים לא משויכים | ⬜ | `/documents/gaps` קיים מ-26.7 — הכלי שם, הטיפול לא נעשה |
| T7 | 40267 — `paid` בלי חשבונית | ⬜ | 40267 חזרה 30.8 כאחת מ-7 ההפקות ההיסטוריות |
| T8 | זנב `unwrap` + `resolveLink` | ⬜ | נוסף מקרה קרוב: `review/route.ts` מחזיר 200 על כשל מלא |
| T9 | זנב הטיפוסים | ⬜ | |
| T10 | בדיקות `profiles` בשני POST | ⬜ | |
| T11 | `/api/shows` לא מאמת `billing_mode` | ⬜ | נוסף: טכנאי משתיק תוכנית דרך `active` באותה משפחת ראוטים |
| T12 | פריסת תא הפעולה | ⬜ | |
| T13 | סף `milestoneApproaching` 47 יום | ⬜ | |
| T14 | ספרינט מהירות רג'יסטרי | ⏸️ | `explain analyze` לפני קוד |
| T15 | חוב בדיקות בשתי סוויטות | 🟡 | `eb0115f` תיקן את משפחת קוד-היציאה. אומת 30.8: **0 אזכורים** ל-`client_review_items` בשני הקבצים — פגם ה-FK עדיין שם, וכך שני האחרים |
| T16 | סריקת bindings נוספים ב-`auth` | ⬜ | אותה סיבת שורש של 0058. איש לא ייראה עד שמישהו ייתקל |
| — | הזמנת עבודה בחוזים | 🟡 | פאנל החוזים (`4508323`) נתן קישור job ידני לאבן דרך — לא הזמנת עבודה |
| — | גלאי הנפקה חיצונית | ⚠️ ⬜ | שני הגארדים של 30.8 הם כפילות **פנימית**, לא הנפקה חיצונית |

**ניקיון:** `ztestcancel_` פתוח · אייקוני PWA לא נגענו · מיילי auth בדשבורד לא נגענו · dev server ישן → התפתח למלכודת שני שרתים החולקים `.next` · `welcome:56` **עדיין פתוח**.

**מבצעי (תזכורות מונחות-אירוע, לא משימות):** 50066 ממתין לתשלום · **עופר "בלי יריה אחת" — אין תקלה**, אבני הדרך ממתינות לתשלום הראשון מהלקוח כמתוכנן (אושר 30.8) · `deal_invoice_enqueue_failed` לא נבדק · 500 בקרון = תזכורת.

**ספירה (נספרה מהטבלה עצמה, 54 שורות):** **11 ✅ מומש · 2 ✅ התייתרו · 8 🟡 חלקי · 33 ⬜ לא נגענו** — מתוכם **14 פיצ'רים** (מסומנים ⚠️). אין טיקטים לא-ידועים ואין פערים ברשימה.

*(תיקון ספירה, 30.8: הדיווח הראשון נקב ב-"50 טיקטים · 14 ✅ · 26 ⬜" — אלה היו הערכות ידניות ולא ספירה. המספרים למעלה נספרו מהשורות. הכיוון לא השתנה, הרזולוציה כן: **המומשים מעטים ב-3 מהמדווח, והלא-נגועים רבים ב-7**.)*

---

## התמונה הגדולה

מה שהתחיל כפיצ'ר אחד ("שם אורח למורנינג") התפצל לשניים, בבשלות שונה:

- **פיצ'ר A — עריכת פירוט רב-שורתית (מנגנון השוער).** שירי עורכת את הטקסט של כל שורת פירוט במסמך `pending` לפני הפקה. הרחבה של מנגנון קיים ובדוק (`/api/documents/pending/edit`). **זה החשוב — הוא רשת הביטחון.** לא תלוי ב-B.
- **פיצ'ר B — הצעת ברירת מחדל אוטומטית (V1b המקורי).** בנייה אוטומטית של `{podcast} · {guest} · {date}` (הפקה בודדת) / `{guest} · {date}` (מאוגד). זו ה"הצעה" ששירי מתקנת. בא **אחרי** A.

**סדר מוסכם:** A קודם, B אחריו. A נותן שליטה מלאה מיד גם עם ברירת מחדל "טיפשה"; B הופך את עבודת שירי מהקלדה לתיקון. אם B נותן פה ושם ערך שגוי — השוער (A) כבר קיים.

---

## למה A קודם (התובנה המרכזית)

הבדיקות חשפו שהמסלול המאוגד לא בשל לאוטומציה:
- **כיסוי 28%** — רק 17 מתוך 60 עבודות מקושרות להפקה. לברק: 2 מתוך 4 שורות.
- **guest מזוהם** — שמות אולפנים ("גבעון") דלפו לשדה guest בפרסר הישן; ידפיסו כשם אורח.
- **תאריך שגוי** — `jobs.date` = יום האישור, לא `record_date` = יום ההקלטה.

כל אלה היו חוסמים **רק** בהנחת שליחה אוטומטית ללקוח. מנגנון השוער (A) מנטרל את שלושתם: שירי רואה, מתקנת, מאשרת. הצעה לא-מושלמת זה בסדר כשבן אדם מאשר לפני שזה יוצא.

---

## פיצ'ר A — ✅ מומש בפרודקשן (`873eb05`, 20.8.2026)

25/25 בדיקות סקריפט + 16/16 בדפדפן (Playwright ב-scratchpad, לא נגע ב-package.json). המסלול הישן מוכח שלא נשבר על מסמך אמיתי. המסד חזר ל-35/60/64 בלי שאריות. שתי החלטות נוספות שהתקבלו תוך כדי ואושרו: (א) `lines` + `description` יחד → 400 (שניהם כותבים ל-income[0]); (ב) `lines` ריק אחרי סינון + נמען לא השתנה → 400 "אין שינוי".

### ההכרעות שיושמו

| # | החלטה | הכרעה |
|---|--------|--------|
| 1 | חתימת שרת | **מערך** `{ id, lines: [{index, description}] }` — שליחה אחת מבטלת מרוץ read-modify-write, ולידציה אטומית לפני כתיבה |
| 2 | מסלול שורה-בודדת | **לא משתנה בכלל** — ענף `if (lines)` חדש לידו. שורה-בודדת ממשיכה לעדכן כותרת + income[0] יחד |
| 3 | חסימת שורה-בודדת בענף החדש | **לחסום** — `income.length ≤ 1` + `lines` → 400. שורה אחת עוברת רק במסלול הישן (שומר על עדכון הכותרת) |
| 4 | כסף | **נעול במצב רב-שורתי** — טקסט בלבד. `lines` + `amount` יחד → 400. עריכת סכום שורה-בודדת נשמרת כמו היום |
| 5 | ניתוק כותרת | **מנותק במסלול החדש בלבד** — עריכת שורה נוגעת רק ב-`income[lineIndex].description`, הכותרת עצמאית |
| 6 | `lineDesc` | **לייצא מ-`bundle.ts`** — מקור אמת אחד ל-preview ולבנייה; מונע חזרת אי-ההתאמה; מכין את B |

**נקודות שינוי:** `edit/route.ts` (ענף lines), `DocumentsClient.tsx` (רשימה מותנית), `FinanceClient.tsx` (יישור preview), `bundle.ts` (ייצוא lineDesc), `test_document_edit.py` (8 מקרים + תיקון באג ניקוי). **אפס מיגרציות.**

**באג שהתגלה ותוקן תוך כדי:** `test_document_edit.py:151-152` בודק שכל טבלת `pending_documents` ריקה — אבל יש 35 שורות אמיתיות → כישלון שווא בכל הרצה. צומצם לשורות שהסקריפט יצר.

---

## פיצ'ר B — ✅ מומש בפרודקשן (`381601f`, 20.8.2026)

27/27 בדיקות טהורות + 22/22 שרשרת אמיתית. הירושה ל-300 מאומתת על נתונים אמיתיים. **הפקה בודדת בלבד** — bundle מחוץ ל-scope בכוונה.

תוצאה בפועל:
- עם אורח: `הזמנת עבודה — אסתטיטוקס · סבטלנה ניקסון · 20.07.26`
- בלי אורח: `הזמנת עבודה — אוכלי סרטים 21.07.26`

השינויים שיושמו: `shortDate()` ב-dates.ts (מניפולציית מחרוזת טהורה, בלי new Date()), `buildLineItemText()` מיוצא ב-enqueue.ts (filter(Boolean).join — מפריד יתום בלתי-אפשרי מבנית), 4 plumbing (טייפ + 2 select + 2 ליטרל), והשמטת מקף יתום בכותרת. מפריד `·` מאומת בשלוש שכבות. אפס מיגרציות.

### ההכרעות שיושמו (לתיעוד)

**פורמט לפי הקשר:**
- הפקה בודדת (100/300 פר-הפקה): `{podcast} · {guest} · {date}` — שם פודקאסט רלוונטי, שורה יחידה.
- מאוגד לפדיון (ברק, מצנע): `{guest} · {date}` — בלי פודקאסט, כי כל השורות אותו פודקאסט (כמו חשבונית 10294).

**דרישות שנקבעו:**
- **מפריד `·` (U+00B7)** — לא em-dash (מתנגש עם `DESCRIPTION_SEPARATOR`), לא מקף רגיל (נבלע בשמות/תאריכים). `·` בטוח, וזה הפורמט שהבעלים כבר כותב ידנית.
- **תאריך `DD.MM.YY`** בשני הענפים (עם אורח ובלי) — עקבי. פורמטר חדש ב-`dates.ts`, מניפולציית מחרוזת טהורה בלי `new Date()` (אזור זמן מזיז יום).
- **spread מותנה** נקי ממפרידים יתומים — בלי אורח, השורה נקייה.
- **record_date פר-הפקה** דרך JOIN `job_productions → productions` (לא מיגרציה ל-0033, לא `jobs.campaign`).
- **description מאוחד** — האורח בכותרת ובשורה כאחד (כמו 10306).

**נקודות שינוי צפויות (7):** טייפ `ProductionForBilling` + 2 select + 2 ליטרל (plumbing), `enqueue.ts:327` (הזרקה, מכסה 100→320 בירושה), `bundle.ts` lineDesc (מאוגד), `dates.ts` (פורמטר). **אפס מיגרציות.**

**מחוץ ל-scope:**
- **מסמך 400 (קבלה)** — אין לו שורות income (0/61). דחוי, אולי הערות בעתיד.
- **חוזים (ICR, אריאל, תשלום מלא)** — חסינים מבנית: `kind='contract'` לא נרשם לתור, אבני-דרך לא מקושרות להפקה → JOIN ריק → פורמט של היום. אין צורך בגארד.
- **ניקוי רטרואקטיבי של guest מזוהם** — לא נדרש; פילטר בזמן בנייה (אם בכלל) מספיק. השוער מכסה בינתיים.

---

## עובדות מפתח מהמסד (מאומתות)

- שורת ההכנסה (`income[].description`) עוברת **בירושה ורבטים** דרך 100→300→305→320 (מאומת על שרשרת ונוס קונספט המלאה). 400 = קיר, אין לו income.
- **אין מסמך מאוגד רב-פרקי קיים** — כל 5 שורות ה-bundle במסד הן job בודד. אין התנהגות לשמר, אין תקדים לאמת מולו.
- מחזור חיים: `pending → approved → issued`. הבנייה והשליחה **לא אטומיות** — חלון pending אנושי, בלי cron. `issuePendingDocument` = הקריאה היחידה למורנינג.
- `payload.income[].description` נשמר ב-jsonb בזמן enqueue, יוצא ורבטים בהנפקה. ניתן לעריכה בחלון.
- ברק = ברק הרשקוביץ, "דעה לא פופולרית" (aliases דל״פ/דלפ), 600/פרק. מצנע = ענבל מצנע, "דברים שלמדתי מנשים מצליחות" (aliases סופרסונאס/מצנע), 500/פרק. חתונמיות — שם מדויק, 600/פרק. כל פרק = job נפרד.

---

## תזכורות עבודה (חוצות-סשנים)

- **פרומפט אחד בכל פעם, דיווח לפני קוד** בנתיבים רגישים.
- **push מאושר בנפרד** — קלוד קוד לא דוחף. תמיד `git status -sb` לאימות ענף, `npm run build` לפני push (ESLint רץ ב-Vercel).
- **מיגרציות: SQL Editor בלבד**, `schema_ledger` אחרי כל אחת. (ל-A ו-B אין מיגרציות.)
- **בדיקה ידנית בפרודקשן אחרי כל טיקט** — הדפוס שחוזר: כל בדיקה ידנית חשפה באג אמיתי שלא היה בלוח (הטריגר 0058, ה-URL/SSO, ה-LOCATION, באג הניקוי בסקריפט).
- מחיקת `finally` מאומתת לפני דיווח הצלחה בכל סקריפט בדיקה.

### 🗓️ 12.9.2026 — לבדוק: ההתראה על סדרת חינוך אמורה להיעלם ב-9.9

ההתראה **"אירוע חוזר ביומן שהמערכת לא קולטת — סדרת חינוך"** היא **false positive ידוע** מ-27.8. אמורה להיעלם **מעצמה בבוקר 9.9.2026**.

**למה היא שם.** הבעלים סיים את הסדרה ב-27.8 (10:59 UTC) והחליף ב-5 אירועים בודדים — **הפער האמיתי סגור**, 1.9/8.9/15.9/22.9/29.9 כולם VEVENTs עצמאיים עם UID משלהם וייקלטו רגיל. אבל **Google לא מוחק master ב"מחק את האירוע הזה והבאים אחריו"** — הוא כותב עליו `UNTIL` ומחריג את השאר:

```
RRULE   FREQ=WEEKLY  ->  FREQ=WEEKLY;UNTIL=20260908T205959Z
EXDATE  8.9 בלבד     ->  1.9 וגם 8.9        (שני המופעים שנשארו — כלומר אפס מופעים חיים)
UID     C77AB6A5-DF21-44E4-BDBE-939DCC125F80   LAST-MODIFIED 20260827T105950Z
```

`isStillRunning` קורא `UNTIL`/`COUNT` ו**לא** `EXDATE` ([`recurring.ts`](../src/lib/calendar/recurring.ts)), רואה `UNTIL` בעתיד, ומתריע. אומת מול הפיד החי: `isStillRunning(8.9)=true`, `isStillRunning(9.9)=false`.

**✅ אם נעלמה — נסגר, למחוק את הפריט הזה.**

**❗ אם ההתראה עדיין שם אחרי 12.9 — זה באג, לא רעש צפוי, וצריך לחקור.** נקודות פתיחה לפי הסדר: (א) האם ה-`UNTIL` על ה-master השתנה שוב — למשוך את הפיד ולבדוק `LAST-MODIFIED`; (ב) האם הרדאר קורא אירוע סנכרון ישן — הוא גוזר מה-`payload` של `cron_sync_completed`/`manual_sync_completed` האחרון, וסנכרון שנכשל/דולג משאיר רשימה מאתמול; (ג) האם `readRecurrence` מפרש נכון `UNTIL` בפורמט הזה. ⚠️ **אל תניח שזה שוב אותו false positive** — ב-9.9 הסיבה הזאת מתה, וכל התראה אחריה היא משהו אחר.

**אם התרחיש חוזר** (סיום סדרות ב-Google הופך לשגרה אצל הבעלים, ולא אירוע חד-פעמי) — **התיקון הצר שווה את זה**: כשלכלל יש `UNTIL`, החלון מהיום עד `UNTIL` חסום וקצר, ומספר המופעים בו הוא חשבון פשוט (`(UNTIL − המופע האחרון שעבר) / interval`). אם ה-EXDATEים מכסים את כולם — הסדרה מתה. דורש לשאת גם `EXDATE` ב-`parse.ts` (היום לא נקרא בכלל), כמה עשרות שורות ובדיקה, **ולא עוזר לכלל אינסופי** — שם אין חלון חסום, ושם ממילא ההתראה נכונה. — *עלה 27.8*

---

## 🎯 30.8.2026 — הפדיון הראשון, וחמישה תיקונים שהוא חשף

**הרגע:** ברק הרשקוביץ, הזמנת עבודה מאוגדת **10317**, 5 פרקים. זה הפדיון הראשון בחיי המערכת — המסלול `createWorkOrderBundle` מעולם לא רץ עד היום (0 אירועי `billing_redeemed`). התוכנית מ-25.8 הייתה "פדה פעם אחת וראה מה יוצא **לפני** שמשקיעים בפורמט", והיא השתלמה: הפדיון עצמו עבד, ו**כל** מה שנשבר סביבו נשבר בגלוי ותוקן באותו יום.

| מה | קומיט | ההכרעה |
|-----|--------|---------|
| **מסלול `consolidated` ב-`forProduction`** | `5612c4e` | חמש ההפקות של 10317 היו **עיוורות למסמך שלהן** — עמודת "הזמנת עבודה" ריקה בכל החמש. `createWorkOrderBundle` כותב `production_id`, `job_id` ו-`bundle_job_ids` כולם NULL (מכוון: הזמנה לא חותמת דבר על job), אז מסלולים 1–3 מתים עליה. הגשר קיים **על הילדים**: כל שורת מקור נושאת `consolidated_into`, ומשם `morning_doc_id` → המסמך במרשם. קדימות: production > job > bundle > **consolidated** > number > receipt. אומת על 10317 החי — `afcc9f77` זהה בשתי הטבלאות, 5 שורות, `shared=true` בכולן |
| **שם האורח כטקסט גלוי בטופס העריכה** | `70488a5` | 3 מ-5 השורות סומנו "אורח חסר" והטופס לא אמר **מי**. שירי יצאה לקנבן וחיפשה לפי תאריך — 9 דקות בין הפדיון (09:23) להנפקה (09:35), רובן חיפוש. והשם היה שם כל הזמן: `guests_by_line` כבר נבנה בשרת, והטופס כבר קרא אותו — **לתוך `title` על תו ⚠ ברוחב 8 פיקסלים**. באג תצוגה, לא באג נתונים. עכשיו: `⚠ אורח ההפקה: עידן טנדלר` + `הפורמט המלא: דעה לא פופולרית · עידן טנדלר · 02.08.26`, מחושב ב-`suggested_by_line` עם `buildLineItemText` עצמה כדי שההצעה לא תסטה מהתקן. **הכיסוי הראשון אי פעם לטופס העריכה** (30 בדיקות) |
| **"צור חשבון עסקה" על שורת הזמנת עבודה במרשם** | `210dbc3` | הכלל `create_deal_invoice_from_work_order` ישב ב-`ALLOWED_CHILDREN` עם `implemented:true` מאז שהבנאי נכתב — ושני ה-`if` ידעו רק `receipt`/`tax`, אז הזמנת עבודה תמיד ענתה "tax" והכלל היה **בלתי-נגיש מהמסך**. `childActionFor` → `childActionsFor` שמחזירה מערך. open-only (237 מ-249 שורות ה-100 סגורות במורנינג). אומת בדפדפן: 12 כפתורים, 0 שגיאות, 10317 בסט המאופשר |
| **0065 — כפילות ליעד הרמן / אוכלי סרטים 28.8** | `e83d54c` | הפקה ידנית על פרק שכבר נקלט מהיומן. שתי הזמנות של 600 ₪ ושני jobs. `merged_into` (לא ביטול — ההקלטה קרתה, רק הרישום כפול), `guest='משה'` הועתק לשורדת, job מוסתר. **9 גארדים, כולם מסרבים במקום להניח.** גארד 4 מסרב לרוץ לפני שהזמנת העבודה הכפולה נדחתה בממשק — דחיית מסמך היא החלטה כספית ושמו של **אדם** צריך להיות עליה, לא שמה של מיגרציה |
| **0066 — כפילות חתונמיות 13.8** | `1a96bcf` | לא זוהתה שבועיים, והייתה מחייבת פעמיים בפדיון ספטמבר. הקומיט נכנס **אחרי** שהמיגרציה כבר רצה, כי `*.sql` ב-`.gitignore` הבליע את הקובץ בשקט. הביקורת המלאה של שלוש הרשימות מופיעה למטה |
| **גארד נגד חשבון עסקה כפול** | `60cbd2b` | מסמך שנוצר מהמרה או מאיגוד נכתב עם `production_id=NULL`, ולכן **אינו תופס את משבצת האינדקס הייחודי**. כשההפקה מגיעה אחר כך ל"אושר ע"י לקוח" — 300 שני, בלי 23505, בלי אירוע. שלושה כללים על ראיה מקומית ומיידית: **א** ל-job יש `invoice_biz` או `invoice_tax` · **ב** שורת תור חיה 300/305/320 מכסה את ה-job · **ג** אותו דבר במרשם. לא "ההזמנה נסגרה במורנינג" — הסגירה מגיעה רק במשיכה היומית, והחלון שביניהן הוא בדיוק מתי שמישהו מקדם את הפרק. 26 הפקות חיות, 5 נחסמות, 0 false-positives |
| **אזהרה לפני יצירה ידנית כפולה** | `67aaa1c` | הקצה השני של אותו כשל: `POST /api/productions` הכניס שורה מיד אחרי אימות ההרשאה ולא בדק דבר. שלוש כפילויות אמיתיות (28.7, 13.8, 28.8), שתיים מהן עלו מיגרציית תיקון נתונים כל אחת. 409 + `needs_confirmation`, והשרת מחזיר את ההפקות **עצמן** — שעה, אורח, סטטוס — כי אזהרה שאומרת רק "כבר יש אחת" היא חיכוך שלוחצים דרכו. שינוי תוכנית או תאריך מאפס את האישור. מתוך 25 יצירות ידניות בהיסטוריה מדליקות **בדיוק שלוש** |

**התבנית שחזרה שלוש פעמים היום:** בדיקה שעברה על צורה אחת לא אמרה דבר על השנייה. הסימולציה הוכיחה את חשבון העסקה המאוגד — והזמנת עבודה מאוגדת הייתה צורה שלישית שאיש לא צפה.

### מה נשאר פתוח מהיום

- **⏭️ 10317 טרם הומר לחשבון עסקה.** ההזמנה הונפקה, הכפתור נבנה ואומת (בסט המאופשר, הרצה יבשה החזירה WOULD BUILD עם 5 jobs תקינים) — אבל ההמרה עצמה לא בוצעה. **זו הפעולה הראשונה בתור.**
- **⚠️ כלל ב' בגארד לא נבחן על נתונים אמיתיים.** זהו הכלל **שהכי חשוב** — והוא מתאים ל-0 שורות היום, כי `invoice_biz` נחתם ב**זמן ההנפקה**: בין "הומר" ל"הונפק" כלל א' שותק וכלל ב' הוא ההגנה היחידה. כלומר בדיוק החלון שהגארד קיים בשבילו הוא החלון שלא נצפה בו מעולם. **להמרה של 10317 יש כאן ערך כפול** — היא מייצרת את השורה הראשונה שכלל ב' יכול לתפוס.
- **7 הפקות היסטוריות נושאות חשבונית ויושבות ב-`עתיד_להתחיל`**: 40217, 40245, 40267, 40275, 40277, 40291, 50058. הגארד **מנטרל את התסמין** — כל אחת הייתה יוצרת 300 שני ברגע שתתקדם — אבל הנתונים עצמם עדיין סותרים: מסמך יצא, הסטטוס טוען שהעבודה טרם התחילה. *(40267 היא T7 בלוח הישן, מאותה משפחה בדיוק.)*
- **`relabel`** — פריט שהבעלים מנה כפתוח, ואין לו הגדרה בריפו ולא בקומיטים. **צריך שורה אחת: מה מתויג מחדש ואיפה.** בלעדיה הוא ימשיך להתגלגל מסשן לסשן בלי שאיש יידע אם הוא נסגר.

### 🔴 0052 אבודה לצמיתות — והכלל שגבה אותה

הביקורת שנעשתה ב-`1a96bcf` על שלוש הרשימות (פנקס / גיט / דיסק):

```
65 גרסאות מספריות בפנקס · 63 בגיט · 64 בדיסק
הוחלו ואינן בגיט : 0052, 0066
הוחלו ואינן בדיסק: 0052
בדיסק ולא הוחלו  : אין
```

**0052 שינתה את `derive_production_status`, הודבקה ידנית ולא נשמרה כקובץ — אין לה קובץ לא בגיט ולא בדיסק, ולא יהיה.** `0054` קיימת **רק** כדי להעתיק בחזרה מהסכימה החיה את מה שאבד. זו הפעם השלישית שהריפו משלם על אותו כשל: 0052, 0066, ו-`0002`/`0005` שנרשמו בפנקס בדיעבד בלי שהורצו — ולכן `archive.jobs` מעולם לא נוצרה. **הכלל `*.sql` ב-`.gitignore` הוא הסיבה, והוא עדיין שם** (ראה טיקט פתוח למטה). 0060–0063 ו-0066 נכנסו ב-`git add -f`; מיגרציה שנשכחת מ-ignore שקט לא מרעישה בשום מקום.

---

## D3 — עקיפת תקרה מבוקרת ✅ בפרודקשן (`1c9c821`, 23.8.2026)

עקיפת אדמין ל-`PULL_NET_CEILING` (10,000 ₪) למסמך יחיד. נכתב בשביל **40258** — אבן דרך ב' של חוזה ידיעות/"מכירת ביפו", 250K נטו. הקוד בפרודקשן, **ההוצאה בפועל ממתינה לתשלום** (בניה בחר לחכות שהכסף יתקבל ואז להוציא 320, לא 305 עכשיו).

- **הרשאה:** `can_manage_users` (בניה, אלי, שירי — כולם מטפלים בחשבונות). בלי מיגרציה.
- **מסמך יחיד + HMAC** — לא דגל מתמשך. קריאה ראשונה מסרבת ומנפיקה כרטיס חתום (TTL 10 דק'), רק ההד מתקבל. "אין confirm להצמיד" — עקיפה בלתי-אפשרית בלי לראות קודם את האזהרה. כרטיס אינו הרשאה (אדמין-כרטיס בידי לא-אדמין → 403).
- **הסכום לעולם לא פרמטר** — יורש ומאומת מ-40258 בשלוש שכבות. העקיפה מדלגת רק על שורת התקרה, כל הגייטים האחרים נשארים.
- **הגנת כפילות מוכחת:** גייט 387 (אידמפוטנטיות) + גייט 443 (invoice_tax על ה-job), בלתי-תלויים — כל אחד עוצר מס שני לבד, גם עם עקיפה מלאה.
- **אודיט:** `tax_ceiling_overridden` (actor, סכום, תקרה, סיבה חובה) לפני הבנייה.

**כשהכסף ייכנס — רצף ההוצאה:** רענן openness של 40258 (raw.ref הוא הסמכות) → הוצא **320** (מס-קבלה, כי אז הכסף התקבל) → אשר → סמן שולם (או קבל התאמה ודאית). אבן הדרך תעבור ל"שולם" אוטומטית.

---

## פאנל החוזים ✅ בפרודקשן (`4508323`, 23.8.2026)

- **השתקת רעש:** חוזה `closed` לא מזין רדאר/התראות/מטריקות (join ל-`contracts.status`, סינון במקור מכסה את 4 הנגזרות).
- **ארכוב:** "סגור חוזה" + "החזר לפעיל" + מתג "הצג סגורים" + תג "כל אבני הדרך שולמו ✓". ברירת מחדל = active בלבד.
- **סטטוס אבן דרך ידני** + **קישור job ידני** עם 3 ולידציות (קיים/אותו לקוח/לא תפוס) ו**אישור כפול** (מה-היה→מה-יהיה).
- **תווית עברית בדרוור** (נגזרת, לא status גולמי).

---

## אוטומציית תשלום + תיקון DRY_RUN ✅ בפרודקשן (`a6ca635`, 23.8.2026)

הכלל העסקי: 305 יכולה לצאת לפני תשלום; **320/400 לעולם רק אחרי תשלום**. לכן הנפקתן מהפכת `paid` אוטומטית.

- **חלק 1 — באג DRY_RUN שהתגלה:** דריי-ראן חתם מספר מזויף בן 6 ספרות (בלי סימון!) על `invoice_tax` של jobs אמיתיים. תוקן — דריי-ראן לא נוגע ב-jobs, עם event `dry_run_jobs_stamp_skipped`. `documents`/`invoices` נשארו (מתויגים `dry-`, נחוצים להיפוך וריאנט).
- **חלק 2 — 320→paid:** `doc_type==='tax_receipt' && job.paid==='לא'` → 'כן', event `via:'auto_tax_receipt'`. פונקציה טהורה `jobPatchForDocument`.
- **חלק 3 — 400→paid:** דרך `linkedDocumentIds` → ה-305 ההורה → ה-jobs שלו, עם dedupe. שלושה events: `job_marked_paid`, `auto_receipt_no_jobs` (הורה בלי jobs — לא שקט), `auto_receipt_paid_without_tax` (אנומליה אמיתית, נשאר אדום ולא מושתק).
- **pull נשאר ידני** (הכרעה קיימת ומנומקת).

---

## תיקון רדאר — accrued_ripe ✅ בפרודקשן (`f46ea37`, 23.8.2026)

ההתראה "אגד חלקי שתקוע 30+ יום ולא יתמלא" צעקה על חתונמיות בטעות — היא ב-4/6 עם פרק טרי, כלומר האגד **כן** מתמלא. התיקון: לבדוק את הפרק ה**חדש** ביותר (`Math.min(agedHours)`), לא הישן — אין פרק חדש 30+ יום = באמת תקוע. מחיר מכוון: אגד שבאמת מת ייתפס יום-יומיים מאוחר יותר; החלפת רגישות באמינות (התראה שצועקת על מצב תקין נלמדת להתעלמות). סקריפט בדיקה ראשון לשלוש התראות ה-cadence (`test_cadence_alerts.py`) — אומת מול הקוד ההפוך (רשת, לא נוף).

---

## ארכיטקטורת job — ✅ בפרודקשן (24–25.8.2026)

**הכלל שהתגבש:** *job הוא עבודה שנעשתה.* לכן הוא נולד ב**הקלטה**, לא באישור הלקוח, ולא לפני שהמיקרופון נדלק. כל שבעת הצעדים למטה הם אותה שורה אחת, נגזרת עד הסוף.

| # | מה | הכרעה |
|---|-----|--------|
| `99d02e8` | קשר ישיר | jobs של מסמך-אב מס נפתרים דרך קישור ה**הפקה**, לא דרך ניחוש. הקשר קיים במסד — הקוד פשוט לא קרא אותו |
| `0060` + `33dc41b` | job ב"הוקלט" | הטריגר יורה גם על מעבר ל`הוקלט`, לא רק על אישור לקוח. + גארד מפני כפילות. **10 פרקים היסטוריים תוקנו** |
| `676a927` | שער בפדיון | פדיון מסרב כשה-jobs שלו כבר חויבו / שולמו / בוטלו. הכשל היה מחזיר 200 עם הערה קבורה |
| `e44c71f` | מילוי אחורה | 9 jobs נוצרו לפרקים הצבורים, לקראת פדיון ספטמבר |
| `0061` | לא לפרק שטרם הוקלט | job לא נוצר לפרק שלא הוקלט — **כולל היפוך המילוי-אחורה** על פרקים שעדיין לא הוקלטו. + סינון זהה בצבירה ובפדיון, מאותו קבוע |
| `0062` + `f55372f` | ביטול = אדמין | ביטול פרק ש**הוקלט** הוא החלטת אדמין, נאכף **בטריגר במסד** ולא ב-route. הביטול גם מסתיר את ה-job |
| `0063` + `eab1acf` | מסלול ההנחה | מסמך `cancelled` משחרר את מקום ה"שורה-חיה-אחת-להפקה", כך שאפשר להוציא מסמך מתקן. + ביטול ל-100, ו-`jobs.amount` מתיישר לסכום שבאמת יצא |

**ההוכחה החיה — לא סימולציה.** דנה ספקטור, "חתונמיות", הוקלטה 24.8. מיומן האירועים של ההפקה:

```
24.8 20:51:35  job_removed_not_recorded      reason: נוצר במילוי אחורה על פרק שטרם הוקלט   ← 0061 מנקה
24.8 21:00:30  client_approved_job_created   fired_by: "recorded"   job 7bab2994  600 ₪    ← 0060 יורה
24.8 21:00:30  status_auto_advanced          בהקלטה → הוקלט
```

הטריגר ירה **באותה שנייה** של מעבר הסטטוס, בלי שאף אחד קרא ל-API. הפנקס: `0060` 24.8 11:37, `0061` 20:50, `0062` 21:25, `0063` 22:04.

---

## דגל "אורח חסר בפירוט" — ✅ בפרודקשן (`2b139ad`, 25.8.2026)

B כותב את האורח לשורה ב**זמן ה-enqueue**, וה-payload קפוא מאותו רגע. לכן שורה יכולה לסתור את ההפקה שלה בשתי דרכים רגילות שאף אחת מהן אינה תקלה בצנרת: השורה נכנסה לתור לפני B והונפקה אחריו (הזמנה 10315 — לתור 20.8 06:01, B נכתב 20.8 23:10, הונפקה 24.8), או שהאורח מולא בדרוור **אחרי** שהשורה כבר בתור, ואף מסלול לא בונה מחדש. כל חמשת נתיבי ה-enqueue נבדקו — **כולם נושאים את האורח כראוי.**

- **פונקציה טהורה** `missingGuestLines(guests, lines, studios)` ב-`guestFlag.ts`. `STUDIOS` הוא **פרמטר**, לא import — אותו חוזה ש-`studios.ts` מגדיר ל-`extractStudioAndGuest`. מקור אמת אחד.
- **התאמה מדויקת מול האולפנים, לעולם לא הכלה.** *המלכודת:* `ד״ר הדר קליין-גבעון` הוא אורח אמיתי במסד ששמו מכיל שם אולפן — `includes` היה מבליע אותו בשקט, על מסמך בדרך ללקוח. בדיקה שמדווחת "תקין" בדיוק על הצורה שבשבילה היא קיימת. הבדיקה בוחרת את השורה **לפי התנאי** ולא לפי שם, כדי שתמשיך להיות בעלת משמעות גם אחרי שהשורה תיערך.
- **רק `income[0]`.** `buildDocumentPayload` שם את שורת ההקלטה ראשונה וכל תוספת אחריה; תוספת ("עריכת ריל") לא נושאת אורח לעולם. מעבר על כל השורות היה מדליק על כל upsell במערכת. מערך ה-guests **מיושר-אינדקס** לשורות, וזה מה שמאפשר לפונקציה אחת לשרת גם הפקה בודדת (`[guest]`) וגם מאוגד (אורח לכל שורה) בלי דגל מצב.
- **מיפוי מאוגד לפי טקסט, לא לפי מיקום.** למאוגד אין הפקה משלו, אז שורותיו נפתרות מהשורות שאוחדו לתוכו דרך `consolidated_into` — ומותאמות לפי **טקסט השורה**, כי `redeem/route.ts` קורא אותן בלי `ORDER BY` והסדר אינו ניתן לשחזור, בעוד הטקסט מועתק בית-בבית.
- **הדגל הוא הכפתור.** "ערוך לפני אישור" ישב על השורה שבועות; מה שחסר היה סיבה ללחוץ. האזהרה קוראת לאותו `openEdit`, והשורה הפוגעת מסומנת בתוך הטופס — **איזו** שורה, לא רק שיש בעיה.
- **אפס מיגרציות, אפס נגיעה בנתיבי enqueue.** האורח לא יושב על ה-payload בכלל; שני המסכים כבר עשו JOIN ל-`productions.guest` — `/documents` אפילו מיפה אותו וזרק.

**הניקוי החד-פעמי** ([`cleanup_studio_in_guest.ts`](../scripts/cleanup_studio_in_guest.ts), נשמר כתיעוד): 5 הפקות נשאו שם אולפן ב-`guest`. 13.8 (לקוח, צבור) → **אלכס קושניר** (ידיעת הבעלים — הכותרת ביומן מעולם לא נשאה אורח, והשם שכן מופיע בה הוא **המנחה**, 5 מתוך 7 כותרות התוכנית פותחות בו). 4 הפקות `internal` → `null`. **אומת: 0 מתוך 325.** שום payload לא נכתב מחדש — `גבעון` מעולם לא הגיע לשורה מודפסת, לא בתור ולא ברג'יסטרי המונפק.

**למה דגל שני ("שם אולפן בשדה האורח") לא נבנה.** הזיהום היסטורי ומתוקן **בשורש**: לפני V1a הפרסר לא הבחין בין אורח לחלל הקלטה, והקונבנציה החדשה (`אורח: X, אולפן`) תוקנה ב-`bba6103`. רות קבסה (23.8) ודנה ספקטור (24.8) הן ההוכחה שהוא לא מתחדש. דגל שיתפוס שורה אחת ואז ישתוק לנצח הוא קוד מיותר.

---

## 🗓️ פדיון ספטמבר — ✅ ברק נפדה 30.8; חתונמיות וסטימצקי בתור

> **עודכן 30.8:** התוכנית שלמטה נכתבה 25.8 והציעה לפדות את **סטימצקי** ראשון (הבדיקה הקטנה ביותר). בפועל נפדה **ברק** — 5 פרקים, הזמנה מאוגדת **10317** — וזו הייתה ההחלטה הנכונה: מסמך בן 5 שורות חשף שלושה כשלים שמסמך בן שורה אחת לא היה מגלה (המסך העיוור למאוגד, שם האורח החבוי ב-`title`, וההמרה הבלתי-נגישה מהמרשם). ראה "🎯 30.8" למעלה. **התחזית שהתאמתה במדויק:** 3 מ-5 השורות סומנו "אורח חסר" — בדיוק אלה שקפאו לפני B (20.8) — והדגל, טופס העריכה והעריכה הרב-שורתית עבדו כפי שנחזה בסימולציה.
>
> **הערת פורמט:** השאלה מסעיף 2 למטה — האם להשמיט שם פודקאסט כשכל השורות מאותה תוכנית — **עדיין פתוחה**. 10317 יצא בפורמט המעורב שנחזה, ושירי תיקנה דרך השוער. ההחלטה אם להשקיע ב-`createWorkOrderBundle` נדחית עד שיהיה מאוגד שני.

**מה מחכה בתור** (`accrued`, נכון ל-25.8; ברק ירד מאז):

| לקוח | פרקים | סכום | אורחים חסרים בפירוט |
|-------|--------|------|---------------------|
| חתונמיות | 5 | 3,000 ₪ | — (4 בלי אורח כלל, 1 תקין) |
| ברק הרשקוביץ | 4 | 2,400 ₪ | **3** (עידן טנדלר, קרן טרנר, אלכס קושניר) |
| סטימצקי | 1 | 800 ₪ | **1** (יואב בלום) |

**הרצף, מאומת בהרצה אמיתית על שורות ברק (והוחזר במלואו):**

```
פדה → HTTP 200, מאוגד status=pending, 4 שורות, 2,400 ₪
     → הדגל מסמן [0,1,2] — 3 מתוך 4, בדיוק החסרות
     → לחיצה על הדגל פותחת את טופס העריכה, 3 קלטים מסומנים והרביעי לא
     → עריכה רב-שורתית → HTTP 200, הפירוט מתוקן, הדגל שותק
     → הסכום לא זז, השורה התקינה לא זזה, כותרת המסמך עצמאית
```

**ההכרעה: העריכה קורית במעמד הפדיון, לא ב-`accrued`.** המאוגד נכנס `pending`, ופיצ'ר A כבר מתיר לערוך `pending`/`failed` — כלומר **לא נדרש קוד חדש**. הדגל ב-`accrued` הוא התרעה מוקדמת בלבד; אין לפתוח שם עריכה.

**פדה סטימצקי ראשון** — פרק אחד, 800 ₪, שורה אחת חסרת אורח. הבדיקה הקטנה ביותר שעדיין מוכיחה את כל השרשרת. *(שים לב: מאוגד בן שורה אחת נופל למסלול השורה-הבודדת — עריכה דרך שדה **תיאור**, לא דרך `lines`, כי הכותרת חייבת לזוז עם השורה. כלל 3 של `873eb05`.)*

---

## ניקוי היסטורי — כשאין מה לעשות

שאריות שהמערכת החדשה לא תייצר, ואין להן מסלול תיקון אוטומטי:

- **9 jobs היסטוריים בלי קישור להפקה ובלי שום מסמך** (לא `invoice_biz` ולא `invoice_tax`) — **⚠️ 5 מהם מסומנים שולמו**, כלומר הכסף נכנס והרישום אצלנו יתום. רובם "פרק N" / "אוכלים סרטים" מיוני–יולי. אין להם הזמנת עבודה ולא תהיה — הזמנה מעוגנת בהפקה. *(הבעלים ציין 8; מהמסד עולים 9 עם אותם 5 ששולמו. שווה מבט לפני שמחליטים מה לעשות איתם.)*
- **`8b4aabe5` — לא נמצא.** חיפשתי את המזהה הזה בכל 80 ה-jobs וב-`productions`/`documents`/`pending_documents`/`clients`/`shows`/`invoices`/`contracts` — אין התאמה. יכול להיות מזהה ישן שנמחק, או שיבוש בהעתקה. **צריך את המזהה הנכון לפני שמטפלים בכפילות.**
- **`*.sql` ב-`.gitignore`** — ראה הפירוט המלא בטיקטים הפתוחים למטה. `0060`–`0063` נכנסו ב-`git add -f`. הכלל הגורף עדיין שם.

---

## 🗓️ לתחילת החודש הבא — פדיון + מסלול מאוגד

**כשמגיע הרגע לפדות את כולם** (ברק: 4 פרקים קיימים + 3 עד סוף אוגוסט = 7; חתונמיות אולי ב-6/6; סטימצקי). התוכנית:

1. **פדה את ברק פעם אחת וראה מה יוצא** — לפני שמשקיעים בפורמט. זה מסמך אחד, עובר תור אישורים, הפיך עד האישור. יענה על שאלת הפורמט **בראיות** ויאמת שהמסלול `createWorkOrderBundle` (שמעולם לא רץ — 0 אירועי `billing_redeemed`) עובד.
2. **הפורמט המאוגד — כנראה כבר לא נדרש כפי שהוגדר.** פיצ'ר B כבר נותן `{podcast} · {guest} · {date}` פר-פרק. השאלה היחידה: להשמיט שם פודקאסט כשכל השורות מאותה תוכנית — עבודה על `createWorkOrderBundle`, לא על "הפורמט הבודד".
3. **ערבוב פורמטים צפוי:** 8/9 השורות הקיימות קפאו לפני B (20.8) → פורמט ישן. אחרי פדיון, חשבונית ברק תערבב ישן+חדש. שירי תתקן דרך השוער (פיצ'ר A), או מילוי-חוזר של השורות הישנות.
4. ~~**זיהום V1a גלוי:** שורה מ-13.8 נושאת `גבעון` (שם אולפן) כאורח.~~ ✅ **נוקה 25.8** — ראה "דגל אורח חסר בפירוט". 0 מתוך 325.

**`createDealInvoiceBundle` הוא קוד מת שנראה חי** — מיוצא, route, UI, בדיקה, ומבנית אי אפשר להגיע אליו (לשונית purple ריקה מבנית). שריד מהארכיטקטורה שלפני 2.8. שווה טיקט: להסיר או להעיר שהוא שריד.

**התראה רביעית (אופציונלי):** `month_closed_unredeemed` מתריע רק **אחרי** שהחודש נסגר (ברק שקט עד 1.9). אם רוצים "X ימים לסוף החודש" (פדיון לפני הסגירה) — `daysToMonthEnd` כבר מחושב ב-`/documents/accrued`, צריך רק לגזור ב-`alerts.ts`, ~8 שורות. **אבל הרדאר כבר עמוס** — ומסך ה-accrued מראה "נסגר בעוד N ימים" עם כפתור פדיון. שקול אם באמת צריך.

---

## ⏭️ הבא בתור (לפי סדר, עודכן 30.8)

1. **להמיר את 10317 לחשבון עסקה.** הכפתור קיים ואומת, ההרצה היבשה החזירה WOULD BUILD עם 5 jobs תקינים — ההמרה עצמה טרם בוצעה. **ערך כפול:** היא גם מייצרת את השורה הראשונה שכלל ב' בגארד יכול לתפוס, והכלל הזה עדיין לא נבחן על נתונים חיים.
2. **`review/route.ts` — בליעת כשל מורנינג בהוצאת מסמכים.** אותו דפוס בליעה שתוקן בעדכון-לקוח (`59837c6`) קיים גם בהוצאת מסמכים — כשל מורנינג מוחזר כ-200. **גבוה, כי זה כסף ישיר** (אתה חושב שחשבונית יצאה כשלא). בירור לפני קוד.
3. **3 רגרסיות בדיקה קלות** (רק רעש, לא סיכון): `test_document_queue` (נתיב שהשתנה), `test_document_date` (פורמט B — בדיקה מצפה לתאריך על 305 שלגיטימית בלי), `test_tax_route_pull` (תקרת D3). לתקן את הבדיקות שיתאימו לקוד החדש.
4. **`*.sql` ב-`.gitignore`** — אחרי ש-0052 אבדה לצמיתות ו-0066 כמעט חזרה על זה, זה כבר לא "פתוח קטן". תיקון של שורה אחת.

## טיקטים קטנים פתוחים (הצטברו מהדרך)

- **UI של 502 partial בדרוור** — עדכון לקוח שנשמר מקומית אבל לא הסתנכרן למורנינג מוצג כשגיאה מלאה ("נשמר אצלנו, אך…" בצבע כישלון). זו חצי-הצלחה, לא כישלון. עבודת UI קטנה.
- **האישור הכפול בעדכון לקוח** — הנימוק השתנה (כבר לא "או שתיהן או אף אחת" אלא "אחת אולי לא תעקוב"). שווה לשקול אם עדיין צריך קליק שני עכשיו שהמקומי נשמר בכל מקרה. (המלצה: להשאיר — מגן על שינוי שמתפשט לכל מסמך עתידי.)
- **מלכודת שני שרתי dev שחולקים `.next`** — עלתה כמה הרצות, נראית בדיוק כמו כשל בדיקה. תזכורת: לפני הרצת בדיקות, לוודא שרץ שרת dev **אחד בלבד**. שווה ל-CLAUDE.

- **`sys.exit` בתוך `finally`** בולע חריגות בכל משפחת סקריפטי הבדיקה — הסתיר שגיאות אמיתיות. מסתיר גם `test_document_date.py` (0 passed) ו-`test_document_recipients.py` (1 passed) שנופלות בשקט. **הכי שווה תיקון** — זה מאמן להתעלם מכשלים.
- **`test_document_queue.py`** — ניקוי חלקי (משאיר `ZTESTDOC`), והטענה `pending_documents is empty` שגויה מול DB חי.
- **לולאת המתנה לשרת** בסקריפטים תופסת רק `ConnectionError`; שרת קר (`ReadTimeout`) נראה כקריסה. תוקן בקבצים החדשים, לא בישנים.
- **`case 7` ב-`test_billing_cadence.py`** מדלג בסביבה הזו — נכון לפי קריאה בלבד, יתאמת בהרצה ראשונה בסביבה שבה רץ.
- **`contract_milestones.status='invoiced'` לנצח** — התצוגה נכונה (נגזרת מה-job), אבל קריאה ישירה של הטבלה מטעה. פער קטן.
- **הפרופיל השארית** `ztestcancel_...` ב-profiles — מחיקה ידנית.
- **תגי `sin1` מיושנים** ב-FUTURE.md ו-productions/page.tsx — הערות בלבד, רענון.
- **400 בלי בדיקת e2e** — בלתי אפשרי במבנה הנוכחי (אין sandbox במורנינג). מכוסה בבדיקות טהורות.
- **טכנאי יכול להשתיק תוכנית שלמה** — `active` יושב ב-`STAGE_FIELDS` של [`shows/update/route.ts:14`](../src/app/api/shows/update/route.ts#L14), כלומר `can_edit_stages` מספיק כדי להעביר תוכנית ל-`active=false` ולהוציא אותה מכל המסכים. שאר מסלולי הארכוב מוגנים טוב יותר: `show_archive` ו-`show_delete` ב-[`approvals/execute.ts:39,108`](../src/lib/approvals/execute.ts#L39) עוברים בתור אישורים, ו-`shows_delete` ב-RLS הוא `can_manage_users`. **פחות חמור מביטול פרק שהוקלט** (0062) — לא נוגע ב-job, לא בכסף, והפעולה הפיכה בקליק — ולכן הוצא במכוון מהיקף תיקון ההרשאות של 25.8 כדי לא להגדיל את הסיכון של מיגרציית הטריגר. **התיקון כשיגיע:** להעביר `active` מ-`STAGE_FIELDS` ל-money/admin, או לנתב אותו דרך תור האישורים כמו `show_archive`. — *עלה 25.8 בבירור הרשאות הביטול*
- **כלל `*.sql` ב-`.gitignore` חוסם מיגרציות חדשות** — השורה נוספה 14.8 עבור קבצי העבודה בשורש (`schema.sql`, `data.sql`, `disable-triggers.sql`…), אבל היא גורפת וגם `supabase/migrations/*.sql` נופל בה. 57 המיגרציות הישנות מתועדות רק כי הן קדמו לכלל, ו-`0059`/`0060`/`0066` נכנסו ב-`git add -f`. **לצמצם שלא יתפוס `supabase/migrations/`** — מיגרציה שנשכחת מ-ignore שקט לא מרעישה בשום מקום: היא רצה במסד, הפנקס נרשם, והקובץ פשוט לא בגיט. הכי קרוב לתיקון: `!supabase/migrations/*.sql` או לצמצם את הכלל המקורי לשורש בלבד (`/*.sql`). — *עלה 24.8 בקומיט `33dc41b`* · **⬆️ הועלה בעדיפות 30.8:** הכלל הזה כבר גבה **מיגרציה שלמה** — `0052` אבודה לצמיתות, ו-`0054` קיימת רק כדי לשחזר את מה שהיא עשתה. `0066` כמעט חזרה על זה. ראה "🔴 0052 אבודה לצמיתות" למעלה. הועבר לתור "הבא בתור", סעיף 4

- **`archive.jobs` ו-`archive.productions` לא קיימות — כל מנגנון "ארכב במקום למחוק" מת** — [`0002:312-314`](../supabase/migrations/0002_atomic_permissions_and_full_schema.sql#L312) מכילה `create schema archive` ואת שתי הטבלאות, אבל `0002` ו-`0005` רשומות בפנקס שתיהן עם `note='backfill'` **באותה חותמת זמן בדיוק** (29.7 12:12:29) — נרשמו בדיעבד כתיעוד של מסד קיים ולא הורצו כקבצים, והמקטע הזה מעולם לא בוצע. `move_jobs_to_archive` ו-`move_productions_to_archive` ([`0005:15,51`](../supabase/migrations/0005_archive_move_functions.sql#L15)) מחזירות `42P01 relation "archive.jobs" does not exist` מאז ומתמיד, ו-[`step2_migrate.py:184`](../scripts/step2_migrate.py#L184) קורא להן. **לא דחוף** — `jobs` מכוסים ע"י `dismissed` (0041) ו-`documents` ע"י `archived_at` על השורה — אבל זה פער אמיתי בין מה שהקוד מבטיח למה שקיים, ו-0064 נאלצה לוותר על ארכוב בגללו. **התיקון:** מיגרציה משלה שיוצרת את שתי הטבלאות + RLS + policies כפי ש-0002 התכוונה. אומת 25.8 בקריאה ל-`move_jobs_to_archive` עם מערך ריק — ה-INSERT עדיין נבנה ומתפוצץ, אפס שורות זזות. — *עלה 25.8 בתכנון 0064*
- **`review/route.ts` מחזיר 200 גם כשכל המסמכים נכשלו** — [`:442`](../src/app/api/documents/pending/review/route.ts#L442) מחזיר `{ ok: results.every(...) }` בלי `status`. הגוף נושא את האמת והמסך מציג אותה (`send()` קורא את `body.results`), אז שירי כן רואה את הכשל — אבל ה-200 משקר לכל צרכן תוכניתי. היישור ל-`59837c6` (**502 + partial**) נדחה במכוון ב-25.8: ערך אמיתי אבל נמוך, כי המסך כבר מכוסה. — *עלה 25.8*
- **בדיקת ה-clash לא סורקת את `documents`** — [`issue.ts:242`](../src/lib/documents/issue.ts#L242) בודק `pending_documents` ו-`invoices` בלבד. אם המשיכה היומית כבר הכניסה מסמך לרג'יסטרי אחרי timeout, ניסיון חוזר לא יראה אותו ויוכל לייצר **מסמך שני** — שאי אפשר למחוק, רק לזכות. נוסח הודעת ה-timeout ("לא ידוע אם המסמך נוצר — בדקי לפני ניסיון חוזר") הוא ההגנה הזמנית, והיא אנושית ולא מבנית. — *עלה 25.8*
- **דדליין לכל האצווה, לא רק לקריאה בודדת** — `MORNING_TIMEOUT_MS` הוא פר-קריאה. אישור מרוכז של N מסמכים תקועים הוא N×15 שניות ויכול עדיין לחרוג מ-`maxDuration=60`. השארית הזאת היא בדיוק מה ששורת `approved` בתור נועדה לתפוס, אבל דדליין לכל הבקשה היה מונע אותה מלכתחילה. — *עלה 25.8*

- **⚠️ מסלול ה-`icsText` מסמן הפקות אמיתיות של היום כ-`calendar_removed`** — כל בדיקה שרצה דרך `POST /api/calendar/sync` עם פיד מזויף פוגעת בנתונים חיים. `runSync` מסנן את `existingByUid` להפקות שה-`record_date` שלהן **היום** ([`route.ts:222`](../src/app/api/calendar/sync/route.ts#L222)), ואז [`sync.ts:86-90`](../src/lib/calendar/sync.ts#L86-L90) מסמן כל אחת מהן שה-`calendar_uid` שלה נעדר מהפיד. פיד מזויף לא מכיל אף uid אמיתי — **ולכן כל ההפקות שהוקלטו היום מסומנות "הוסרו מהיומן"**, ובנוסף נכתב `calendar_flagged_removed` על כל אחת. אומת 26.8 מול המסד: הפקה חיה אחת (דעה לא פופולרית, 26.8) הייתה נפגעת מהרצת בדיקה אחת. הדגל גלוי למשתמש על הכרטיס, ולא מתנקה מעצמו — הסנכרון האמיתי הבא מבטל אותו רק אם האירוע עדיין ביומן, ואם ההפקה כבר נגעה בה יד היא נופלת ל-`toFlagChanged` במקום. **איך `test_calendar_recurring_alert.py` מגן על עצמו** (התקדים לחיקוי): מצלם `id,calendar_removed` לכל הפקות היום **לפני** קריאת הסנכרון הראשונה, משחזר ב-`finally` **לפני** כל ניקוי אחר, מאמת `drift=0`, ומוחק רק את אירועי ה-`calendar_flagged_removed` שנכתבו אחרי `STARTED_AT` — כדי שלעולם לא ימחק אירוע שהקרון האמיתי של 06:00 רשם. **התיקון האמיתי כשיגיע:** להעביר ל-`runSync` דגל שמכבה את ענף ה-removed כשהמקור הוא `icsText` — הבדיקה בודקת יצירה והתאמה, ולעולם לא את "נעלם מהיומן", שדורש פיד אמיתי כדי להיות בעל משמעות. חלופה זולה יותר אך שברירית: להזריק את ה-uid-ים האמיתיים של היום לפיד המזויף. עד אז — **כל בדיקה חדשה דרך המסלול הזה חייבת לחזור על מנגנון ההגנה, אחרת היא משאירה נזק שקט**. — *עלה 26.8 בכתיבת התראת האירועים החוזרים*

- **`relabel`** — פריט שהבעלים מנה כפתוח (30.8), **ואין לו הגדרה בשום מקום בריפו** — לא בקומיטים, לא בטיקטים, לא בקוד. חיפוש מלא לא העלה דבר. **צריך שורה אחת שאומרת מה מתויג מחדש ואיפה.** בלעדיה הוא יתגלגל מסשן לסשן בלי שאיש יוכל לומר אם נסגר. — *עלה 30.8*

*(עופר / "בלי יריה אחת" **הוסר מכאן 30.8** — אין תקלה ואין משימה. אבני הדרך ממתינות לתשלום הראשון מהלקוח, בדיוק כמתוכנן. חי כתזכורת מונחית-אירוע ב-[`TICKETS.md`](TICKETS.md) "מבצעי" בלבד.)*

**אזהרת תהליך:** יש שני ריפואים (`bizi-app`, `bn-invoice`). פקודות git ידניות — לוודא `pwd` לפני commit/push, ה-cwd החליק פעם אחת.

---

## מה נשאר פתוח (אפיקים)

**מאוגד (bundle) — מחוץ ל-scope בכוונה.** הפורמט המאוגד `{guest} · {date}` (בלי שם פודקאסט, כמו חשבונית ברק 10294) לא מומש. ⚠️ **חסם לפני שנוגעים:** לשונית "לא חויב" ריקה — 0 עבודות — כי כל עבודה לא-מחויבת כבר נושאת `invoice_biz`. זו הסיבה ש-`createDealInvoiceBundle` מעולם לא הפיקה מסמך: אין לו קלט. לפני השקעה בפורמט המאוגד צריך להבין איך ומתי עבודות מגיעות למצב לא-מחויב, אחרת בונים למסלול שאף אחד לא עובר בו.

**~~ניקוי V1a רטרואקטיבי~~ — ✅ סגור 25.8.** 5 הפקות נוקו, אומת 0 מתוך 325. הזיהום לא מתחדש (הפרסר תוקן ב-`bba6103`), ולכן **לא נבנה דגל** — ראה "דגל אורח חסר בפירוט". שים לב שהפילטר "בזמן בנייה" שנשקל כאן **לא** מומש ולא נדרש: `buildLineItemText` כותב את מה שבשדה, והשדה נקי.

**V1c — פתוח.** רענון `guest`/`studio` בענף העדכון של סנכרון היומן. שינוי בכותרת אחרי יצירה לא מגיע להפקה. לא נגעו.

**תזכורת V1a:** 0 מתוך 2,680 כותרות ביומן מכילות `אורח:`/`אורחת:`. הפורמט החדש יראה אורחים רק כשהכותרות ייכתבו כך. עד אז כל שורה = `{תוכנית} {תאריך}`.

---

## 🗓️ סנכרון היומן מפספס אירועים חוזרים (RRULE) — הוכרע: לא מתקנים, מתריעים

**מה קרה (26.8).** פרק של "גיא ואור" מ-25.8 14:00 לא נקלט. הסנכרון רץ תקין באותו בוקר (`cron_sync_completed`, `created=1`) ורץ שוב ידנית שלוש פעמים באותו יום — ומעולם לא ראה את האירוע.

**הסיבה.** [`parse.ts:20-46`](../src/lib/calendar/parse.ts#L20-L46) אינו מרחיב `RRULE`. גוגל מייצא אירוע חוזר כ-**master אחד** + VEVENT נפרד רק למופעים ששונו (`RECURRENCE-ID`). `new ICAL.Event(vevent).startDate` על master מחזיר את המופע **הראשון**, ולכן רק תאריך ה-master נכנס לחלון היומי. האירוע:

```
SUMMARY  סדרת חינוך - אור גיא ומיכאלי - גבעון גדול
UID      C77AB6A5-DF21-44E4-BDBE-939DCC125F80
DTSTART  20260818T140000     RRULE  FREQ=WEEKLY   (בלי UNTIL — אינסופי)
EXDATE   20260908T140000
```

18.8 נקלט (זה ה-DTSTART של ה-master), 25.8 נעלם, 1.9 ייקלט **במקרה** — הבעלים הזיז אותו ל-17:00 ולכן גוגל חומר לו VEVENT משלו. אומת בהרצת הפרסר האמיתי:

```
2026-08-18: 1 אירוע שורד את חלון היום -> הפקה נוצרה
2026-08-25: 0 אירועים                 -> שקוף לחלוטין
2026-09-01: 1 אירוע שורד (override)   -> הפקה תיווצר
```

**זה לא הכינויים ולא ה-internal.** `matchTitleToShow` הורץ על הכותרת המדויקת ומתאים (`אור גיא ומיכאלי`, 15 תווים, מנצח את `סדרת חינוך`); ל-28.7/12.8/18.8 כותרת זהה בייט-לבייט וכולן יצרו הפקות. `internal` נגזר **אחרי** ה-INSERT ולכן אינו חוסם יצירה.

**היקף על הפיד החי:** 2688 VEVENTs · 20 recurring masters · 21 overrides · 12 masters נושאים EXDATE. **בדיוק אחד** מתאים לתוכנית פעילה. אבד מופע אחד (25.8), 16 מופעים עתידיים עד 31.12. הבא שייעלם הוא **15.9** — 8.9 מסומן `EXDATE` ולא אמור להיווצר בכלל.

**ההכרעה (בעלים, 26.8): לא מתקנים.** הקלטות קבועות נדירות אצלו; הוא ייצור אירועים בודדים. במקומו נבנית **התראה בלבד** שהופכת את הכשל השקט לגלוי — ראה הסעיף הבא. ההפקה של 25.8 הושלמה ידנית: `6eec89b0-0c03-403e-b73f-3fcc8d5660e4`, סטטוס הוקלט, בלי job ובלי מסמך (נכון — `on_production_approved` יוצא מוקדם על `kind<>'client'`).

### התיקון המלא — שמור למקרה שנצטרך

אם אי־פעם נחזור להרחבה, זה הבירור המלא. **ההרחבה עצמה היא ~15 שורות; העבודה האמיתית היא הזהות.**

**1. ⚠️ החסם: ה-INSERT הנופל.** הזיהוי הוא לפי `calendar_uid` בלבד ([`sync.ts:67`](../src/lib/calendar/sync.ts#L67)), ויש **אינדקס ייחודי חלקי**:

```sql
CREATE UNIQUE INDEX productions_calendar_uid_key ON public.productions
  USING btree (calendar_uid) WHERE ((calendar_uid IS NOT NULL) AND (split_count IS NULL));
```

מופעים של סדרה חולקים UID. `existingByUid` מסונן ל-`record_date = today` ([`route.ts:222`](../src/app/api/calendar/sync/route.ts#L222)), ולכן מופע שני נראה חדש → INSERT על uid תפוס → **23505** → [`route.ts:316`](../src/app/api/calendar/sync/route.ts#L316) עושה `throw` → **כל ההרצה של אותו בוקר נופלת**, וכל האירועים שאחריה אובדים. זה גרוע מהבאג המקורי. **האינדקס והקוד חייבים לעלות באותו deploy**, ורצוי להחליף את ה-`throw` בטיפול שלא מפיל את הלולאה.

**זהות מורכבת — שתי אפשרויות:**
- `(calendar_uid, recurrence_id)` — נאמן ל-RFC5545, אבל דורש עמודה חדשה (אין כזו — נבדקו כל 43 העמודות), backfill, והכנסת העמודה ל-`guard_production_calendar_columns` אחרת נפער חור הרשאות.
- `(calendar_uid, record_date)` — **בלי עמודה חדשה.** אומת על נתונים חיים: 31 הפקות מחזיקות `calendar_uid`, **0 כפילויות, 0 התנגשויות** תחת הצירוף. נשבר רק אם סדרה חוזרת פעמיים באותו יום (`FREQ=HOURLY`/`BYHOUR`) — לא קיים בפיד.

בשני המקרים: לשמור את החריגה `split_count is null` ([`0019`](../supabase/migrations/0019_multi_episode_session.sql#L11-L14) קבעה במכוון ש-split siblings **חולקים** uid), ולוודא ש-`toFlagRemoved` ([`sync.ts:86-90`](../src/lib/calendar/sync.ts#L86-L90)) משתמש באותו מפתח — אחרת הפקה חוזרת תסומן "הוסרה מהיומן" כל יום.

**2. ⚠️ מלכודת ה-exceptions ב-ical.js — 420 קישורים שגויים.** הבנאי ([`event.js:69-77`](../node_modules/ical.js/lib/ical/event.js#L69-L77)) מקשר exceptions אוטומטית אבל **סורק את כל ה-VCALENDAR ולא מוודא UID**:

```
default ctor     -> 21 exceptions related   (כל override בפיד, כל UID שהוא)
explicit+strict  -> 1 exception             (האמיתי)
```

20 masters × 21 overrides = **420 קישורים שגויים**, ויש כבר **התנגשות אמיתית אחת** בפיד: `"איל ואייל בגבעון"` במופע `2026-01-29T10:00:00` היה לוקח את השעות שלו מ-`"ג׳ייסון הקלטה בחשמונאים"`. ו-`strictExceptions: true` **זורק** על הפיד הזה (`Error: attempted to relate unrelated exception`), כי מסלול ה-auto-relate מזין לו את כל ה-overrides. הצורה **היחידה** שעובדת:

```ts
new ICAL.Event(vevent, { strictExceptions: true, exceptions: overridesByUid.get(uid) ?? [] })
```

זה נכשל **בשקט** אם מפספסים — שעה שגויה על הפקה, לא שגיאה.

**3. דילוג על overrides.** `getOccurrenceDetails(occ).item !== ev` מזהה מופע שיש לו VEVENT משלו. בלי זה 1.9 ייווצר פעמיים: פעם מההרחבה (14:00, שגוי) ופעם מה-VEVENT שלו (17:00, נכון).

**4. EXDATE — בחינם.** `RecurExpansion` מטפל ב-`exdate` ו-`rdate` נייטיב ([`recur_expansion.js:412`](../node_modules/ical.js/lib/ical/recur_expansion.js#L412)). אומת: מופע מסומן EXDATE לא נפלט מהאיטרטור. 12 מ-20 ה-masters נושאים EXDATE, כולל שלנו — בלי זה ניצור הפקה לפרק שהבעלים ביטל.

**5. RRULE אינסופי.** `FREQ=WEEKLY` בלי `UNTIL` = איטרטור אינסופי. צריך להזריק את `israelDayWindow` **פנימה** לפרסור (עצירה כש-`occurrence >= end`, דילוג כש-`< start`), להשתמש ב-`iterator(startTime)` כדי לא לדרוך על 7 שנות מופעים (ה-master של "אוסנת" מ-2019), ולהוסיף guard מונה קשיח נגד RRULE פתולוגי.

**6. פרסור.** `new ICAL.Event()` על 2688 אירועים = **239ms** (הבנאי סורק את כל ה-VCALENDAR לכל אירוע — O(n²)). זה כבר המצב היום ונסבל; מעבר מלא נוסף יכפיל.

**חתימה שמשתנה:** `parseIcsText(text)` → `parseIcsText(text, window)`, ואיתה שני הקוראים ב-[`route.ts:474`](../src/app/api/calendar/sync/route.ts#L474) ו-[`:508`](../src/app/api/calendar/sync/route.ts#L508) שמסננים היום *אחרי* הפרסור.

## 🔔 התראה: אירוע חוזר שהמערכת לא קולטת — מתוכנן, טרם נבנה

זיהוי בלבד. בלי הרחבה, בלי איטרטור, בלי מיגרציה, בלי שינוי סכימה.

**⚠️ נקודת התכנון הקריטית:** הזיהוי חייב לרוץ על **`allEvents`, לפני מסנן חלון היום**. ה-master של סדרת חינוך נושא `DTSTART=18.8`, ולכן ב-26.8 הוא מסונן החוצה ב-[`route.ts:474`](../src/app/api/calendar/sync/route.ts#L474) לפני שהוא מגיע ל-`runSync` בכלל. התראה שיושבת אחרי המסנן תצפצף בדיוק יום אחד בשנה. הפתרון: `runSync(events, date, allEvents)` — פרמטר אחד נוסף, והזיהוי משתמש ב-`showsForMatch` שכבר נטען שם.

**שני הפילטרים (שניהם זולים, שניהם אומתו):**
1. `matchTitleToShow` מול תוכניות **פעילות** — כבר רץ ממילא, `showsForMatch` כבר מסונן ל-`active`.
2. **"עדיין רץ"** מקריאת מאפייני ה-RRULE בלבד: `UNTIL` → חי אם `>= today` · `COUNT` → חי אם `DTSTART + (COUNT-1)×INTERVAL×freqDays >= today` · אין אף אחד → אינסופי → חי.

תוצאה על הפיד החי: **20 masters → התראה אחת.** שני הפילטרים מרוויחים את מקומם — התאמת-תוכנית מחסלת 18, ו"עדיין רץ" מחסל את `"בן של מלך - גבעון"` שכן מתאים לתוכנית פעילה (דניאל זמיר ומלך) אבל פג ב-4.2026. בלעדיו היו שתי התראות, אחת מהן מתה.

*אי-דיוקים מוצהרים:* חשבון ה-`COUNT` מקרב (`MONTHLY≈30`, `YEARLY≈365`) ומתעלם מ-`BYDAY` — זו טריאז' ולא דיוק, והשגיאה נוטה לכיוון התרעה. `isRecurring()` מחזיר true גם ל-`RDATE`-only; אין כאלה בפיד היום, ומטופלים כ"חי" (שמרני).

**שלא תצעק כל יום — בלי שורה חדשה בכל בוקר, ובכלל בלי שורה חדשה.** הרדאר **נגזר** (`getRadarAlerts` מחשב בזמן טעינה, לא קורא טבלת התראות). לכן: כל הרצת סנכרון כותבת את הרשימה **הנוכחית** לתוך ה-payload של `cron_sync_completed` / `manual_sync_completed` שהיא כותבת ממילא — `recurringUnsynced: [{uid, showId, showName, title}]` לצד `skippedNoMatch`. הרדאר קורא את אירוע הסנכרון המושלם **האחרון** וגוזר ממנו.

- אפס שורות חדשות, אפס שינוי בטבלה append-only.
- **מתנקה מעצמו** — הבעלים מחליף בסדרת אירועים בודדים → הבוקר הבא לא כולל אותו ב-payload → ההתראה נעלמת. בלי כפתור "טופל", בלי מצב לתחזק.
- המונה היומי רוכב על השורה הקיימת (`recurringUnsyncedCount`), בדיוק כמו `skippedNoMatch`.
- *הסתייגות:* אם הסנכרון נכשל/דולג, הרשימה האחרונה היא מיום קודם. יש תקדים לניסוח — `order_not_closed` כבר כותב "נכון לסנכרון האחרון".

**הנוסח:** `אירוע חוזר ביומן שהמערכת לא קולטת — {תוכנית}. צור אירועים בודדים` · severity **yellow** (דורש פעולת בעלים; `blue` שמור למצבים תקינים־אך־ראויים־לידיעה כמו `produced_not_billed`) · `href: /settings`.

**היקף:** 4 קבצים, בלי מיגרציה — `parse.ts` (לשאת `recurrence` על `CalendarEvent`, קריאת מאפיינים בלבד) · `lib/calendar/recurring.ts` חדש וטהור (`isStillRunning` + `findUnsyncedRecurring`, באותה רוח כמו `match.ts`/`sync.ts` שהם טהורים ובדיקים) · `route.ts` (פרמטר + שני שדות ב-summary) · `alerts.ts` (התראה אחת נגזרת). מסלול ה-`icsText` ב-POST כבר קיים ומכסה master / UNTIL-שפג / COUNT-שמוצה / אינסופי / תוכנית-לא-פעילה בלי לגעת ביומן החי.

**סיכון: נמוך.** מסלול הזיהוי **חסין** למלכודת ה-420 — הוא לא בונה exceptions ולא מרחיב, רק קורא מאפיינים. אין נגיעה בכסף: התוכנית היחידה שמפעילה אותו היא `internal`.

---

## מסלול הקומיטים (V1)

| קומיט | מה |
|--------|-----|
| `4760d35` | docs — V1 אובחן, פוצל ל-V1a/V1b/V1c |
| `bba6103` | V1a — חילוץ אולפן ואורח מכותרת היומן |
| `873eb05` | פיצ'ר A — עריכת פירוט רב-שורתית ב-`/pending/edit` |
| `9457a8f` | backlog V1b |
| `6674492` | ניקוי מלכודת `<ref>` אחרי הסרת קישור סינגפור |
| `381601f` | פיצ'ר B — אורח + תאריך קצר בשורת הפירוט |
| `1c9c821` | D3 — עקיפת תקרה מבוקרת (HMAC, אודיט) |
| `4508323` | פאנל החוזים — השתקת רעש, ארכוב, סטטוס+job ידני |
| `a6ca635` | אוטומציית 320/400→paid + תיקון באג DRY_RUN |
| `f46ea37` | תיקון רדאר — accrued_ripe מודד את הפרק החדש |
| `729146b` | backlog — תיקון רדאר + תוכנית חודש הבא |
| `eb0115f` | תשתית בדיקות — קוד יציאה נקבע אחרי הניקוי, לא ב-finally (חשף 20 כשלים מוסתרים) |
| `59837c6` | תיקון — עדכון לקוח נשמר מקומית גם כשסנכרון מורנינג נכשל (partial) |
| `caafd3b` | backlog — תיקון תשתית בדיקות, מדיניות סנכרון לקוח, תור המחר |
| `99d02e8` | ארכיטקטורת job — jobs של אב-מס נפתרים דרך קישור ההפקה |
| `33dc41b` + `0060` | ה-job נוצר בהקלטה, לא רק באישור + גארד כפילות + 10 היסטוריים תוקנו |
| `676a927` | שער בפדיון — סירוב כשה-jobs כבר חויבו / שולמו / בוטלו |
| `e44c71f` | מילוי אחורה — 9 jobs לפרקים הצבורים, לקראת פדיון ספטמבר |
| `1316923` + `0061` | אין job לפרק שטרם הוקלט + כלל מוכנוּת אחד משותף לצבירה ולפדיון |
| `f55372f` + `0062` | ביטול פרק שהוקלט = החלטת אדמין, נאכף בטריגר במסד; הביטול מסתיר את ה-job |
| `eab1acf` + `0063` | מסלול ההנחה — `cancelled` משחרר את מקום ההפקה, ביטול ל-100, `jobs.amount` מתיישר |
| `2b139ad` | דגל "אורח חסר בפירוט" בשני המסכים + ניקוי 5 ההפקות המזוהמות |
| `a170b1e` + `0064` | ה-job מתוארך ליום שבו העבודה נעשתה |
| `dee1fa8` | מסמך שנתקע ב-`approved` נחשף, ועצירת התלייה מול מורנינג |
| `7651e2e` | התראה על אירוע חוזר ביומן שהמערכת לא קולטת — זיהוי בלבד |
| `8024497` | מסך `/projects` — טבלה כרונולוגית של הפקות + פותר המסמכים |
| `e83d54c` + `0065` | כפילות ליעד הרמן / אוכלי סרטים 28.8 — מיזוג והסתרת ה-job |
| `5612c4e` | מסלול `consolidated` ב-`forProduction` — הזמנה מאוגדת נמצאת דרך הילדים |
| `70488a5` | שם האורח והפורמט המלא כטקסט גלוי בטופס העריכה |
| `1a96bcf` + `0066` | כפילות חתונמיות 13.8 + ביקורת פנקס/גיט/דיסק שחשפה את 0052 |
| `210dbc3` | "צור חשבון עסקה" על שורת הזמנת עבודה במרשם |
| `60cbd2b` | גארד ב-`enqueueDocument` נגד חשבון עסקה כפול (3 כללים) |
| `67aaa1c` | אזהרה לפני יצירה ידנית של הפקה ליום שכבר נקלט מהיומן |

**תזכורת על V1a:** 0 מתוך 2,680 כותרות היומן עומדות בקונבנציה החדשה כרגע. V1a יחזיר אורחים רק כשתתחיל לכתוב `אורח:` / `אורחת:` בכותרות. זה גם אומר שברירת המחדל של B (בלי אורח) היא המסלול הרגיל עד שהכותרות ישתנו.

**V1c** (רענון `guest`/`studio` בעדכון סנכרון) — לא נגעו. פתוח.

---

## D2 — סגור (סינגפור נשאר)
נבדק 20.8: השהיה חסומה ב-Pro, סינגפור תורם ~$2-6/חודש בלבד (רובו מקוזז בקרדיט Pro). **ההחלטה: להשאיר, לא מצדיק העברת ארגון/מחיקה.** מה שכן נעשה — תיקון `.temp`/`<ref>` (`6674492`) שמנע באג CLI עתידי. אין משימה פתוחה. **תובנה שנשמרת:** לחיפוש env/סודות להשתמש ב-`command grep`, לא ב-wrapper שמכבד `.gitignore` בשקט.
